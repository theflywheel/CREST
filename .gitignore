.DS_Store
node_modules/
.env
.env.*
!.env.example
*.log
.venv/
__pycache__/
dist/
/tmp/

# DeDi node signing keys and the locally built dedid binary. Never committed.
infra/compose/dedi-keys/

# `go build ./services/<name>/` drops a binary named after the package at the
# repo root. One of them (`registry`) was committed in eec03af — a 12MB Mach-O
# that only builds on one architecture, tracked as though it were source.
/registry
/parties
/definitions
/evidence
/confirmation
/verification
/payments
/notify

# PixelPass is pulled at card-render time; the lockfile is checked in.
tools/spikes/printedcard/node_modules/

# Stray build artefacts from `go build ./tools/...`
/poc
/seed
/oidc
tests/e2e-apps/node_modules/
tests/e2e-apps/test-results/
# The fidelity gate's per-screen verdicts and the screenshot-pair contact
# sheet: both are outputs of a run against a live stack, regenerated by
# `make fidelity` / `make fidelity-sheet` and never committed.
tests/e2e-apps/fidelity-results.jsonl
docs/.fidelity-sheet/

# recorded journey demos — regenerated, never committed
demos/
