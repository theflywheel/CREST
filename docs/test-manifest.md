# Test manifest

Every feature, and how we know it works. Maintained **by hand** — see [TESTING.md](TESTING.md) for why.

**Status values:** `planned` (no code yet) · `partial` (some layers proven) · `spike` (a check exists and passes, run on demand rather than in CI) · `covered` (every listed check passes in CI) · `unproven` (code exists, validation does not — treat as a bug).

**How to use this file**
- Adding a feature? Add its row in the same change. A feature with no row is unproven.
- Closing an issue? Its manifest rows must be `covered`, or the issue says explicitly why not.
- Reviewing? Check the row, not the diff's optimism.

---

## W1–W10 — worker invariants

These are the promises to the worker. They must be executable at G3 (#33); until then they are `planned` and that is a debt with a date on it. Blueprint §11.

| ID | Invariant | How it is proven | Layer | Status |
|---|---|---|---|---|
| W1 | Work recorded is work that happened | Evidence with no source record is rejected; harness asserts no unit exists without a canonical record | E2E | planned |
| W2 | A worker sees what was recorded about them, before it counts | Confirmation notification asserted for every draft claim; no claim reaches ACTIVE unnotified | E2E | planned |
| W3 | Silence is not consent against the worker | Auto-confirm at T=7 still releases payment **and** stays disputable afterwards | Unit + E2E | planned |
| W4 | A dispute never costs the worker their money | All four T=7 exits assert payment release; dispute path asserted explicitly | Unit + E2E | planned |
| W5 | A disputed claim never destroys the underlying unit | Unit survives claim contest; re-claim possible by another party | Unit | planned |
| W6 | A worker's record is portable and verifiable without CREST | Printed card verifies offline with all CREST services stopped | E2E | planned |
| W7 | No disclosure without consent, per request | Verification without consent artefact rejected; refusal recorded as a value, not an error | E2E | planned |
| W8 | A worker can see who checked them | Check audit trail contains every verification, including refused ones | E2E | planned |
| W9 | Identity is never over-collected | Assert no raw national ID or biometric in any store; only pairwise ref + salted hash | Contract + E2E | planned |
| W10 | A held payment always has a reason with an owner | Every reconciliation gap has owner + reason; the cleared-but-never-instructed deadline rule fires | E2E | planned |

**The W9 check runs against the actual datastore**, scanning for anything resembling a raw identifier. An invariant about what is *absent* cannot be proven by testing the happy path.

---

## Foundations

The scaffolding. These are `covered` because the code exists and the checks run — the only rows in this file that are.

| Feature | Issue | How it is proven | Layer | Status |
|---|---|---|---|---|
| Injectable clock | — | Advance 7 days in microseconds; backwards travel; concurrent read/advance under `-race`; System clock is UTC | Unit | covered |
| A driveable clock still ticks | — | `pkg/clock/offset_test.go` — `Offset` reads real time plus a skew: it moves with its base, keeps running after `Set`, and stacks `Advance` on top of elapsed real time. `Fake` (frozen) stays for unit tests. Found in the deployed demo, whose clock was frozen at the seeder's last step: every screen read fine and nothing in the stack could ever become due again | Unit | covered |
| A seeded stack is handed back on real time | — | `tools/seed` with `SEED_LIVE_CLOCK` (default: on whenever `SEED_STORY` is) seeds an epoch `StoryClockAdvance` in the past, walks the story forward, then posts `{"live":true}` to `/internal/clock` on every service. Proven end to end by `make test-e2e-sweep` | E2E | covered |
| Only `pkg/clock` reads wall time | — | `forbidigo` rule; verified by a deliberate violation failing lint | Lint | covered |
| Services cannot import each other | — | `depguard` rule in `.golangci.yml` | Lint | covered |
| DB drivers confined to `pkg/store` | — | `depguard` rule; verified by a deliberate violation failing lint | Lint | covered |
| Repository layout | — | `make structure`; verified by a stray directory and an unknown service both failing | Lint | covered |
| Health and readiness endpoints | — | Both respond per service; health reports the **injected** clock, so a service silently reading wall time is caught | Unit | covered |
| Service images build and run | — | Distroless image builds; container starts; `/healthz` answers over HTTP | E2E | covered |
| Per-service Postgres schemas | — | All seven schemas created on first boot; no cross-schema foreign keys | E2E | covered |
| Mock rail enforces idempotency | — | Same key twice → one instruction, replay header on the second | Contract | covered |
| Mock rail injects real failure modes | — | `timeout` mode: caller sees 504 while the rail recorded `cleared` — the cleared-but-never-confirmed case W10 must survive | Contract | covered |
| Mock SMS records messages | — | Sent messages readable per recipient, so "the worker was notified" (W2) is assertable without a phone | Contract | covered |

## P0 spikes

Spikes prove things about **other people's software**, so they live outside the harness and are marked `spike` rather than `covered` — they are run deliberately, not in CI, and they retire when the thing they de-risked is wrapped by CREST code. Findings: [p0-findings.md](p0-findings.md).

| Feature | Issue | How it is proven | Layer | Status |
|---|---|---|---|---|
| DeDi publish → inclusion proof | [#2](../../issues/2) | `make spike-dedi`: record published, fetched with `?proof=inclusion` | Spike | spike |
| Inclusion proof validates independently | [#2](../../issues/2) | `tools/spikes/dediproof` — a second implementation, stdlib only. Proven by **tampering**: altered leaf digest, rewritten `created_by`, flipped audit-path step and wrong verifier key are each rejected | Spike | spike |
| Definition versions are immutable | [#2](../../issues/2) | v2 published; v1 still resolves by `version_id` **and its original proof still validates** — a credential issued under v1 stays verifiable | Spike | spike |
| Upstream image tags exist and match our platform | [#1](../../issues/1), [#3](../../issues/3) | Every tag in `.env.example` checked against the registry and pulled. Found `inji-verify:0.11.0` does not exist and the component is two images | Spike | spike |
| Registry spike against the **deployed** node | [#2](../../issues/2) | `make spike-dedi-deployed` — same script, real node, public origin. Caught a proof-checker bug that every local run had passed | Spike | spike |
| `WorkEventCredential` issued over OpenID4VCI | [#1](../../issues/1) | `make certify-issue` — against the **deployed** Certify: authorization-code flow through eSignet, `openid4vci-proof+jwt` holder proof, credential returned. Nine assertions | Spike | spike |
| The issued credential carries provenance, not a tier | [#1](../../issues/1) | `make certify-issue` — asserts `sourceClass, captureMethod, adapterRef, receivedAt, sourceExposure` all present, and that no `tier`, `trustTier`, `nationalId`, `uin`, `individualId`, `face` or biometric field appears anywhere in it (W9; "trust strength is derived, never stored") | Spike | spike |
| The credential's proof verifies against the issuer's published key | [#1](../../issues/1), [#164](../../issues/164) | `make certify-issue` — the wallet credential now signs `Ed25519Signature2020` (#164): the script asserts the suite and, against a deployed fleet, that **Inji Verify** answers VALID (proven against the production fleet 2026-09-02 — the DID must end `:.well-known`, see `docs/DEPLOYMENT.md`); `tools/spikes/verify-2020.py` re-verifies the proof independently (URDNA2015 + Ed25519) **and proves the tamper case** — a changed outcome is rejected, so the served `@context`'s `@vocab` is keeping every field under the signature. The old JCS branch remains for stacks still on `eddsa-jcs-2022` | Spike | spike |
| The credential is bound to the holder's own key | [#1](../../issues/1) | `make certify-issue` — `credentialSubject.id` is the `did:jwk` of the key that signed the proof-of-possession | Spike | spike |
| The wallet is offered the credential | [#1](../../issues/1) | `/v1/mimoto/issuers/CREST/configuration` on the deployed Mimoto returns `WorkEventCredential` — which requires Certify's well-known **and** eSignet's authorization-server metadata to both resolve (findings C11, C12) | Spike | spike |
| Printed PixelPass card → offline verify | [#66](../../issues/66) | `make printed-card` renders a card from a credential issued by the deployed Certify and asserts it decodes back byte-identical; `make offline-verify-sealed` checks its Ed25519 proof from local files in a container run with `--network none`. Run on demand, against a live issuer | Spike | spike |
| A credential CREST did not sign verifies here | [#66](../../issues/66) | `pkg/credential/interop_test.go` — a real Certify-issued credential and the DID document Certify serves, checked in as fixtures. Deleting the `@context` line in `signingInput` fails it. Tampering with the outcome fails it. The fixture carries no identifier and no tier | Unit | covered |
| Credential in a wallet → browser download → printed card in a hand | [#66](../../issues/66) | **Not proven.** The wallet download through Inji Web and the physical scan — printed, real device, radios off — still need a person, a printer and a phone | E2E | planned |
| eSignet deployed and answering discovery | [#3](../../issues/3) | `make verify-deployed` — discovery is served over the public URL and `issuer` matches the host it was fetched from | Spike | spike |
| eSignet advertises only pairwise subjects | [#3](../../issues/3) | `subject_types_supported == ["pairwise"]` in the discovery document. **Configuration, not behaviour** — the rows below are the behaviour | Spike | spike |
| eSignet subject is stable across sessions | [#3](../../issues/3) | `make spike-esignet` — two full authorization-code round-trips per client, same `sub` both times | Spike | spike |
| eSignet subject is partitioned by relying party | [#3](../../issues/3) | `make spike-esignet` — a client under a different `relyingPartyId` receives a different `sub`; two clients under the same one receive the same `sub` (finding E6) | Spike | spike |
| The id_token carries no national identifier | [#3](../../issues/3) | `make spike-esignet` — decoded id_token claims are `sub, aud, acr, auth_time, iss, exp, iat, nonce, at_hash`; `individual_id` is never requested (W9) | Spike | spike |
| eSignet relying-party registration in a real deployment | [#53](../../issues/53) | Not a test, and not P0. A partner conversation, blocked on choosing a pilot geography; the mobile-OTP provider class is the stated fallback | — | planned |
| `individual_id` is never requested or stored | [#3](../../issues/3) | The claim is offered (finding E2). Proven when the relying-party registration exists and a test asserts the requested claim set excludes it (W9) | Contract | planned |

## Deployment

| Feature | Issue | How it is proven | Layer | Status |
|---|---|---|---|---|
| Deployed fleet answers, allowlist holds | [#138](../../issues/138) | `make verify-deployed` sweeps all seven services through the crest-web proxy, asserts unknown names and `/internal/*` 404 (§16 fence), checks all seven public doors, then the DeDi and eSignet checks; the deploy workflow's matrix covers the same fleet and polls each member's observable readiness path — `/readyz` for the services, wired to the database ping — failing if it never answers. Proven green against Railway production 2026-08-27, re-proven after the readiness fix from dough's #141 review | Deploy | covered |
| Deployed log is independently verifiable | — | `make verify-deployed` validates a real inclusion proof with our own verifier, against a key cross-checked against the checkpoint signature | Deploy | covered |
| `main` deploys only after CI passes | — | `deploy.yml` triggers on CI **conclusion == success**, not on push | Deploy | covered |
| Commit messages are machine-checked | — | `commitlint` in a lefthook `commit-msg` hook; unknown scopes and non-conventional subjects rejected | Lint | covered |
| Secrets cannot reach a commit | — | `guard-secrets.py --scan` in the lefthook `pre-commit` hook, sharing its pattern table with the agent hook | Lint | covered |
| Rollback | — | **Not proven.** Railway redeploy exists; no tested path back to a previous version | Deploy | planned |
| Backup and restore | — | **Not proven, and not configured by us.** A backup nobody has restored from is a belief | Deploy | planned |

## Infrastructure layer

| Feature | Issue | How it is proven | Layer | Status |
|---|---|---|---|---|
| Eleven primitive schemas | [#12](../../issues/12) | `make test-contract` — every schema compiles and is reachable by `$id`; the fixture world validates against all of them at load | Contract | partial |
| Go types cannot drift from the schemas | [#46](../../issues/46) | `make generate-check` in CI — `tools/codegen` regenerates `pkg/schema` and fails if the committed file differs | Lint | covered |
| The fixture world can be moved to today's dates | [#40](../../issues/40) | `harness/fixtures/rebase_test.go` — `LoadAt` slides every RFC3339 instant in the world by one delta, so intervals survive exactly and only the anchor moves; `Load` leaves the file's dates alone, because a scenario asserting on a date must not depend on the day it runs. Without this, a deployment seeded months after the file was written runs a programme whose authorizations have already expired | Contract | covered |
| A definition authorises the issuer its deployment actually signs with | [#40](../../issues/40) | `harness.withIssuer` unit tests, and the seeder asks `GET /v1/issuer` rather than trusting the fixture's name. Proven by seeding a stack with `ISSUER_ID=did:crest:issuer:railway-demo` and verifying a story credential: `valid: true`. Found on the deployed PoC, where every credential the story issued had always failed verification with "not an authorised issuer" — the fixture names `did:crest:issuer:local` and that deployment signs as `railway-demo`. The fixture's own comment warns that this mismatch is not harmless; the deployment fell into it regardless, which is why the value is now resolved rather than declared | E2E | covered |
| A fixture worker already bound to a subject can still be logged in by a scenario | [#102](../../issues/102) | `harness/scenarios/identity_test.go` `login` gained the enrolment-agent fallback `story.login` already had: when self-bind is refused because the worker arrived bound, the supervisor binds on their behalf. Proven by the full e2e run going from 18 `403 subject_not_enrolled` failures to green | E2E | covered |
| A sibling service reads authorizations without a caller token | [#102](../../issues/102) | `GET /internal/authorizations` twin in parties; the issuer's `firstAuthorization` (confirmation's until #137 moved issuance to verification) calls it instead of `/v1/authorizations`. Proven by `TestACredentialNamesTheChainAVerifierWalks`, which failed with a trust chain truncated to a bare org id when the guarded route silently 403'd the best-effort read | E2E | covered |
| A valid token is not authority to mint a grant | [#124](../../pull/124) | `createAuthorization` requires the caller to prove they are — or act for — the grant's `authorityPartyId`, and refuses a non-organisation authority. `TestAValidTokenIsNotAuthorityToMintOrRevokeGrants`: minting in the org's name and self-authorising both answer 403. The seeder now self-binds to the org party (first-login bootstrap) to keep fixture grants loadable; `approvedByPartyId` stays a recorded fact, because the fixture world holds a grant approved by a person under the org's authority | E2E | covered |
| A stranger cannot revoke another party's grant | [#124](../../pull/124) | `revokeAuthorization` loads the grant and runs the same authority gate before touching it. Same scenario: a signed-in worker revoking the org's grant by id answers 403, while `TestARevokedGrantStopsNewWorkButNotWorkInFlight` still proves the org itself can | E2E | covered |
| The apps proxy refuses services that are not CREST's | [#124](../../pull/124) | `nginx.apps.conf.template` carries the same explicit `crest-*` allowlist and `/api/` 404 fallback as `nginx.conf.template`; an open `(?<svc>[a-z-]+)` on a shared Railway project proxies to anything in it. Config symmetry, checked by eye against the apps' `config.js` service names — no automated test drives the Railway nginx | manual | partial |
| A failed sweep exit stays visible as a shortfall in `due` | [#124](../../pull/124) | `due` is counted from what WAS owed (`dueWindows` + unreached), not from successful outcomes, so a claim whose exit failed leaves `due > autoConfirmed + heldForSomeoneToLookAt` — the shortfall the OpenAPI contract promises. Happy-path counts asserted by `spine_test.go` and the scheduled-sweep scenario; the failure branch itself is unexercised — no scenario makes an exit fail mid-sweep, and confirmation has no unit-test seam for it | E2E | partial |
| A sweep and a worker confirmation racing at T=7 exit once | [#124](../../pull/124) | `TestASweepAndAConfirmationRacingAtT7ExitOnce` fires `/v1/sweep` and the worker's self-confirm from the same starting line on a due window; both must succeed, and the record must show one exit route, one credential for the claim, one payment instruction. The exit path serialises on a `FOR UPDATE` lock of the window row; the loser takes the idempotent branch | E2E | covered |
| A rival authority cannot overwrite a grant by reusing its id | [#124](../../pull/124) | The create upsert reads any existing row under `FOR UPDATE` and refuses with `409 grant_id_taken` unless it stands under the same authority — a caller who passes the gate for their own organisation cannot replace another organisation's grant through it. `TestAValidTokenIsNotAuthorityToMintOrRevokeGrants` runs the overwrite as a rival organisation and asserts the 409 and that the victim's grant still permits | E2E | covered |
| A grant is replayed exactly or refused — never edited in place | [#124](../../pull/124) | A duplicate id is accepted only when the submitted document equals the stored one byte for byte; a same-authority variant answers `409 grant_not_editable`, because an in-place rewrite would update `doc`/`state`/`review_by` while the indexed permission columns keep the old row — a grant whose document disagrees with `permits()`. The scenario tries the edit (409) and the exact replay (201) | E2E | covered |
| An organisation-shaped party is not an authority until APPROVED | [#124](../../pull/124) | The grant gate reads the registration, not the party's kind: `POST /v1/parties` is the open bootstrap door, so the attack scenario creates an organisation party, first-binds to it, and is refused `403 organisation_not_approved` when minting. The seeder now walks the fixture organisation through application → terms → custodian decision, and scenario organisations onboard the same way | E2E | covered |
| Two first logins racing one unbound party bind once | [#124](../../pull/124) | The self-proof decision is re-made inside the append transaction under a `FOR UPDATE` lock on the party row; the loser answers `403 party_already_bound`. The same lock closes a lost-update where two concurrent appends could silently drop a binding. `TestTwoFirstLoginsRacingOneUnboundPartyBindOnce` fires both logins from one starting line and asserts one winner, one 403, one binding | E2E | covered |
| Re-seeding a persistent stack tolerates the standing grant | [#125](../../pull/125) | The demo seeder rebases fixture dates to "now" each run, so its grant can never be a byte-exact replay of the one an earlier seed stored; the registry answers `409 grant_not_editable` and is right to. The seeder accepts that answer only after reading the standing grant (`GET /v1/authorizations/{id}`, authority-only) and comparing the whole document against the fixture with only the rebased time fields (period, approvedAt, reviewBy) normalized — terms, evidence, functions, scope, state and revocation must match byte for byte; any drift fails the seed loudly (dough reviews of #125, #134 and #135: a field-list predicate kept missing what the full document catches). Proven by seeding a live-clock world twice over one database: both runs complete, five grants permit-checked and left as is | E2E | covered |
| The seed hold outlives the deadlock detector | [#126](../../pull/126) | `SEED_HOLD=true` held with `select {}`; once every other goroutine went idle the Go runtime declared "all goroutines are asleep - deadlock!" and killed the process, so the platform restart-looped the one-shot seeder. The hold now sleeps on a ticking timer. Proven by running the seeder with the hold for 240s against a live stack — past the ~90s window the deployed one died in — and finding it still holding. Retired 2026-08-28 (#139): crest-seed is a run-on-demand job since #136, so the hold is unreachable and the code is gone; the row stays as the record of why the job model exists | E2E | retired |
| The window length is configuration, not a constant | [#132](../../pull/132) | `CONFIRMATION_WINDOW` is L2 policy (#127/#128). `TestAShortWindowPaysByRealTimeAlone` runs a stack at `CONFIRMATION_WINDOW=6s SWEEP_EVERY=2s` and proves a window opens, auto-confirms, issues the credential and releases payment by real time alone — no clock driving, no manual sweep. The existence proof that #129 can remove clock control from the infrastructure | E2E | covered |
| A grant is readable by its authority alone | [#135](../../pull/135) | `GET /v1/authorizations/{id}` exists for the seeder's shape check and answers only the grant's own authority — a grant read by id names a subject and their functions, which for a person is a record of who works where (#68). `TestAGrantIsReadableByItsAuthorityAlone`: the authority reads its grant back; a stranger with a valid token gets 403 | E2E | covered |
| Issuance lives in the substrate, not the payments application | [#137](../../issues/137) | `TestIssuanceLivesInTheSubstrateNotThePaymentsApplication` — confirmation 404s `/v1/issuer`, `/v1/status-list` and `/v1/credentials`; verification publishes the issuer key. The positive half is the whole existing spine: every exit now lands its credential through `POST /internal/credentials/issue` on verification (idempotent by claim), and the full e2e suite is the proof that nothing about the worker's experience changed | E2E | covered |
| A dispute revokes a crash-orphaned credential | [#143](../../pull/143) | `TestADisputeRevokesACrashOrphanedCredential` — dough's finding on #143: a confirming exit can crash after verification commits the credential and before the window records the exit; a dispute of the still-open window must not leave that orphan standing. The scenario plants the orphan by issuing directly, disputes, and asserts a dispute exit with no credential id, payment released, and the orphan revoked. A dispute after auto-confirm still leaves the credential standing (W3), asserted elsewhere | E2E | covered |
| The payments application is one service | [#129](../../issues/129) | `confirmation` and `payments` merged into one deployable (`payments`) with one schema boundary; windows and contests copied from the old schema by migration `0002_windows.sql`; the release still crosses the outbox with an idempotent consumer, so a crash between exit and instruction still cannot lose a payment. Proven by the full e2e suite green from a clean stack (`make test-e2e` — the merged migrations build the world from nothing) and every window/dispute/sweep scenario running against the merged service | E2E | covered |
| The issuance seed is custody, not configuration | [#130](../../issues/130) | `pkg/config` `SecretStr`: with `VAULT_ADDR` set the seed comes from Vault KV or the signer refuses to start — a sealed Vault, a missing field, or half a configuration all fail loudly, never falling back to the environment (unit: `vault_test.go`, four refusal cases plus the read). The compose stack runs a dev-mode Vault holding the seed and `verification` boots from it, so every e2e run proves the custody path; proven negatively too — vault stopped, verification restarted, and the signer crash-looped on "issuer seed unavailable" rather than booting on an unescrowed key | Unit + E2E | covered |
| The canonical fixture world | [#40](../../issues/40) | `make test-contract` — `tests/fixtures/world.yaml` loads, every object validates, and every named id resolves | Contract | covered |
| Trusted Payments profile | [#13](../../issues/13) | The three LinkedRecord payload schemas exist and validate, and no primitive schema mentions payments. Profile-needs-no-primitive-change not yet asserted mechanically | Contract | partial |
| Canonical evidence record | [#14](../../issues/14) | `tests/contract/adapter_test.go` — every record the CSV adapter emits validates against `schema.IDEvidenceRecord`, and a record with only the mandatory core is valid | Contract | covered |
| Provenance is adapter-attached, never source-asserted | [#14](../../issues/14) | `tests/contract/adapter_test.go` against `csv-asserting-its-own-provenance.csv`: a file's own `source_class`/`capture_method`/`adapter_ref` columns are ignored and kept as enrichment (§8) | Contract | covered |
| A batch loses no rows | [#22](../../issues/22) | `adapters/csv/csv_test.go` — every data row leaves as a record or a named rejection, and the reference points at the real line even after a quoted newline | Unit | covered |
| CSV batch adapter | [#22](../../issues/22) | `adapters/csv/csv_test.go` — six bad-value cases reject one row each; a missing mandatory column fails the file and names it | Unit | covered |
| Source heartbeat / silence detection | [#14](../../issues/14) | `harness/scenarios/heartbeat_test.go` — a registered feed goes quiet, the alert fires once per episode rather than once per sweep, its owner is told, and a feed that resumes reads healthy again. A cadence and an owner are both required at registration | E2E | covered |
| Which key matched is kept, not just that one did | [#14](../../issues/14) | `harness/scenarios/spine_test.go` reads `claim.matched.key` back after a batch that joined on a phone number and requires `contact-route`. A match nobody can explain is a match nobody can dispute | E2E | covered |
| Idempotency and replay semantics | [#14](../../issues/14) | `TestTheSameRecordTwiceCollidesOnTheDedupeKey` — the same work down a second transport is one unit; six distinguishing mutations must not collide. Receipt time and transport are not inputs | Unit | covered |
| Strength function `f` | [#15](../../issues/15) | `pkg/strength/testdata/vectors.json` — eleven cases run as a table, readable by a second implementation in any language | Unit | covered |
| Transport never affects strength | [#15](../../issues/15) | One set of facts through all four transports must give one answer (§8) | Unit | covered |
| A credential resolves against its pinned definition version | [#15](../../issues/15) | The same facts under v1 and a stricter v2 give different tiers, and v1 does not move | Unit | covered |
| Assurance improving later raises the tier, with nothing reissued | [#15](../../issues/15) | `TestAssuranceImprovingLaterRaisesTheTierWithNoReissuance` — one set of stored provenance facts evaluated at IA-1 and IA-3; the second must be strictly higher (§4.1) | Unit | covered |
| A re-assessed source downgrades without reissuance | [#15](../../issues/15), [#6](../../issues/6) | A `SourceAssessment` cap changes the tier for an unchanged credential, and the reasoning says so | Unit | covered |
| External-institution evidence caps at Tier 2 | [#15](../../issues/15) | `pkg/strength/testdata/vectors.json` — an institution's own system record reads tier 2 and its hand-keyed row still reads tier 1, so the distinction G1 #8 exists to make survives the cap. The §16 ruling lives in `f` rather than a definition's map: as L2 data the same credential would mean different things in two deployments | Unit | covered |
| The cap is on one source class, not on everybody | [#15](../../issues/15) | A national-system vector still reaches tier 3. A change that quietly lowered every tier would pass a test that only checked the capped case, and the symptom would be every worker's record reading weaker than it is | Unit | covered |
| Declining offered work is recorded nowhere | [#15](../../issues/15) | `tests/contract/decisions_test.go` — no primitive schema may mention a decline. A negative decision is invisible in a diff until somebody adds the field for a good local reason, so §16's ruling gets a test that fails when it is violated, carrying the reasoning where whoever trips it will read it | Contract | covered |
| WorkEventCredential issuance | [#16](../../issues/16) | `make test-e2e` — a confirmed claim issues a credential that verifies, carries no tier and no national identifier. **Issued by CREST's own signer, not Certify** — see the row below | E2E | partial |
| Credential signature and tampering | [#16](../../issues/16) | `pkg/credential` — a signed credential verifies; reassigning the subject, re-pointing the proof at another key, or verifying under a stranger's key all fail | Unit | covered |
| Issuance through Inji Certify | [#16](../../issues/16) | **Not proven for CREST's own credentials.** Certify does issue a `WorkEventCredential` over OpenID4VCI — see the P0 spike rows — but from a CSV fixture, not from CREST's evidence store, and nothing validates that fixture against the schemas. The CREST-side path is unbuilt | E2E | planned |
| Credential revocation | [#16](../../issues/16) | `make test-e2e` — revoking flips a bit in the signed Bitstring Status List and verification refuses the credential; the list is fetched whole so the check reveals nothing | E2E | covered |
| Identity provider interface | [#17](../../issues/17) | `TestEachProviderClassAssertsOnlyWhatItChecked` — all six classes map to the level the check behind them can honestly assert, through one contract. `POST /v1/parties/{id}/identity-bindings` takes any of them | Unit | covered |
| A binding appended later raises assurance, reissuing nothing | [#17](../../issues/17) | `harness/scenarios/registry_test.go` — a field-enrolled worker reads IA-0, binds eSignet, reads IA-3 from the endpoint that derives it fresh. The unit-level pair is `TestBindingAnAnchorLaterRaisesAssuranceWithNothingReissued` | E2E | covered |
| Bindings are append-only; history is never rewritten | [#17](../../issues/17) | An identical binding re-sent appends nothing; the same provider with a different subject appends a second record and the first still says what it said | E2E | covered |
| An expired binding is no level, not a lower one | [#17](../../issues/17) | `TestAnExpiredBindingIsNoLevelRatherThanALowerOne` — and the expired record is still present to be read | Unit | covered |
| An identifier held by another worker holds, never merges | [#17](../../issues/17) | `TestAnIdentifierAlreadyHeldByAnotherWorkerIsRefusedRatherThanMerged` — 409 naming the reason, and the refused party's assurance is unchanged | E2E | covered |
| A roster id survives a later write to the party | [#17](../../issues/17) | `TestAddingABindingDoesNotUnregisterTheWorkersRosterID` — the key rebuild used to delete roster ids, whose only symptom would be that worker's evidence arriving unclear from then on | E2E | covered |
| Six OpenAPI descriptions | [#17](../../issues/17) | `schemas/openapi/*.yaml`, one per service, referencing `schemas/` by `$id` rather than restating it. Hand-written from the handlers — nothing yet checks they have not drifted | Contract | partial |
| Identity assurance is derived, never stored | [#20](../../issues/20) | `registry` computes it from a Party's bindings on request; there is no column to store it in (§4.1) | E2E | covered |
| Bednet definition, three faces | [#18](../../issues/18) | Seeded through the real endpoints, ratified and activated by the harness, and its tier map drives both the strength vectors and the live verdict. `harness/scenarios/registry_test.go` asserts its DeDi publication carries a digest and a version | E2E | covered |
| A definition's author is never its ratifier | [#21](../../issues/21) | Enforced in the definitions service and in a CHECK constraint; the harness seeds through DRAFT → ratify → activate, so the path is exercised on every run (§7) | E2E | covered |
| A caller's identity is established, not asserted | [#89](../../issues/89) | `pkg/identity` verifies an OIDC access token — signature against the issuer's JWKS, issuer, audience, expiry, not-before — with the algorithm allowlist passed to the parser so `alg: none` and a symmetric key checked against a public one are unrepresentable rather than caught. `TestATokenIsRefusedWhenAnythingAboutItIsWrong` runs seven ways of being wrong; `TestAnUnsignedTokenIsNotEvenParsed` is the eighth | Unit | covered |
| The provider's subject is re-salted before anything holds it | [#89](../../issues/89), [#52](../../issues/52), [#63](../../issues/63) | `TestPairwiseSeparatesDeploymentsAndProviders` — two deployments produce different subjects for one eSignet `sub`, two providers issuing the same `sub` do not collide, and the issuer and subject cannot run together. `TestAValidTokenEstablishesACaller` asserts the raw `sub` never reaches the Caller | Unit | covered |
| Withdrawing a worker's consent requires being them | [#89](../../issues/89) | `harness/scenarios/identity_test.go` — unauthenticated is 401, another worker is 403, the worker themselves succeeds, and after each refusal the consent still reads GRANTED. This is the endpoint #89 opens with: one call used to make somebody's work stop counting (§9) | E2E | covered |
| The assisted paths still work, and are recorded as assisted | [#89](../../issues/89) | `TestASupervisorWithdrawsForAWorkerWhoCannotDoItThemselves` — a worker reachable only through their supervisor has their consent withdrawn by that supervisor, who holds `act-for-party` scoped to the project. `TestActingForSomebodyElseNeedsTheAuthorization` is the unit pair, and asserts both the refusal and that a permitted assistance reports itself as assisted | Unit + E2E | covered |
| The header is not the permission | [#89](../../issues/89) | `TestAWorkerCannotActForAnotherWorkerJustByAsking` — `X-CREST-On-Behalf-Of` from a caller with no `act-for-party` authorization is 403, and the target's consent is untouched | E2E | covered |
| A confirmation's exit route is what happened | [#89](../../issues/89) | `TestAConfirmationCannotClaimToBeSomethingItIsNot` — a caller acting alone cannot record an assisted route, and the window stays open after the refusal so the worker has not lost their chance to confirm. Before this, a request could say `self` while being anybody, and the record then read as the worker's own confirmation of work they were never asked about | E2E | covered |
| The supervisor-assisted T=7 exit, end to end | [#89](../../issues/89) | `TestASupervisorConfirmsForAWorkerAndItIsRecordedAsAssisted` — a supervisor holding `act-for-party` confirms for a worker, a credential is issued, and the window's exit route reads `assisted`. An assisted confirmation stored as the worker's own is indistinguishable afterwards from one they never made | E2E | covered |
| An approval names somebody who proved it | [#89](../../issues/89) | `TestAnOrganisationApprovalNamesSomebodyWhoProvedIt` — the self-approval rule was a constraint on a value the caller chose, satisfiable by naming anybody. Naming the specifier while authenticated as the applicant is now 403 `party_not_proven` | E2E | covered |
| An authenticated stranger is told to enrol, not rejected | [#89](../../issues/89) | `TestAnAuthenticatedStrangerIsNotAnybodyHere` — somebody the provider knows and this deployment does not gets 403 `subject_not_enrolled`, which is a state to be enrolled out of rather than an error to retry. `TestDenialSeparatesTheThreeRefusals` keeps 401, unbound and not-permitted from collapsing into one status | Unit + E2E | covered |
| One subject is never two people | [#89](../../issues/89) | `TestOneSubjectCannotBeTwoPeople` — 409, held by the service and by `party_keys_one_party_per_subject` in migration 0008, so a future code path that forgets still cannot write the row | E2E | covered |
| A token's life cannot be extended by moving the clock | [#89](../../issues/89) | `pkg/identity.NewVerifier` reads the wall clock in every environment and takes no clock parameter. A driveable clock set to last March would otherwise make every expired token valid again, and whoever can move the clock could then log in as anybody who ever held one | Unit | covered |
| A forged token does not become a request to the identity provider | [#89](../../issues/89) | `TestARefetchIsRateLimited` — twenty tokens naming keys that do not exist cause at most two key fetches. `TestAKeySetIsCachedAndRefreshedOnAnUnknownKey` proves the other half: an issuer rotating its key does not need a restart | Unit | covered |
| A production deployment cannot start unauthenticated | [#89](../../issues/89) | `TestAProductionDeploymentWillNotStartWithoutAnIdentityProvider` — the rule is a function (`startupRefusal`) so it can be tested without a process that exits, and `TestAnUnconfiguredServiceRefusesRatherThanAllows` asserts that a service with nothing to ask says no rather than yes. The `os.Exit` itself is not exercised | Unit | covered |
| A worker's history is continuous across a merge | [#100](../../issues/100) | `harness/scenarios/merge_test.go` — two records of one person each collect work, both are confirmed and paid, and after the merge the survivor's claims, confirmation windows and payment instructions each return both. The pre-merge separation is asserted first, so a read that returned everything regardless could not pass this | E2E | covered |
| A merge is followed on the read, never written into history | [#100](../../issues/100) | The same scenario asserts a claim still names the absorbed party after the merge. Rewriting those rows would destroy the only record of what the system believed before it corrected itself, and a claim that said whose work it was at the time is a true statement | E2E | covered |
| A stale link naming an absorbed id still works | [#100](../../issues/100) | `TestAskingAboutTheAbsorbedRecordGivesTheWholeHistory` — the printed cards and saved links from before a merge name the old id, and answering "that record is gone" would be the system punishing a worker for its own correction. `GET /v1/parties/{id}/identifiers` returns the survivor and the chain from either end | E2E | covered |
| A worker with no merge reads exactly as before | [#100](../../issues/100) | `TestAPartyThatWasNeverMergedReadsTheSameAsBefore` — every list read now asks the registry which ids are this person, and the unmerged case is the one that would break quietly | E2E | covered |
| An incomplete history is refused rather than served | [#100](../../issues/100) | `TestAnIncompleteHistoryIsRefusedRatherThanServed` — the registry is SIGKILLed and the claim list answers 503 `registry_unavailable` rather than a shorter history. The tempting fallback to the single id returns a hole with nothing saying anything is missing, and the caller believes it | E2E | covered |
| A worker's confirmation history is answerable at all | [#100](../../issues/100) | `GET /v1/windows?partyId=` — the service could previously only be asked about one claim, which made the merge gap invisible here rather than absent. `partyId` is required: a list of every window in the deployment is a report, not a worker's record | E2E | covered |
| The service is `parties`; "registry" means the log or the faces | [#50](../../issues/50) | The whole suite runs under the new name (compose service, schema, env keys, harness), twice consecutively against one stack. §13 records the vocabulary: "the log" (DeDi), "the public faces" (the ten registries), `parties` (the service) | E2E | covered |
| A deployment crosses the rename without a data migration | [#50](../../issues/50) | `AdoptLegacySchema`: a database holding schema `registry` and not `parties` has it renamed on first boot (`FormerName`). Proven 2026-08-25 against the deployed Railway database: the merge deploy renamed the schema in place — `parties` exists, `registry` is gone, the publication row and drained outbox carried over, healthz answers as `parties`. CI databases are always fresh, so nothing re-runs this path automatically; it is a one-shot migration that has now happened | Deploy | spike |

## The spine (G2, #25)

Proven by `make test-e2e`: docker compose up, seed through the real endpoints, drive the clock, tear down. Three consecutive clean runs required (#40).

| Feature | Issue | How it is proven | Layer | Status |
|---|---|---|---|---|
| CSV in, verifiable credential out | [#25](../../issues/25) | `TestARecordBecomesACredentialAndAPayment` — a batch becomes a unit and a claim, the worker is notified, confirming issues a credential that verifies, and a payment instruction is released for the right amount | E2E | covered |
| The E2E harness itself | [#40](../../issues/40) | `make test-e2e` from a clean checkout with no manual step; readiness by polling, never `sleep`; the clock is driven rather than waited on | E2E | covered |
| W2 — the worker is told before it counts | [#23](../../issues/23) | The window and its notification commit together; the SMS arrives and says the worker will be paid either way | E2E | partial |
| A worker who was never reached is not auto-confirmed against | [#23](../../issues/23) | `TestAnUnreachedWorkerIsNotAutoConfirmedAgainst` — the notification outcome is recorded on the window, the sweep skips `unreached` windows and reports them, and the supervisor-assisted route closes them. Found by review: notify answers 201 for a failed send, so "notified" never meant "told" | E2E | covered |
| A re-submitted batch does not pay twice | [#22](../../issues/22) | `TestResubmittingTheSameBatchDoesNotPayTwice` — a unit's identity is derived from the work it describes, so two ingestions converge. Found by review: the unit id was minted fresh each time and the schema's own comment claimed otherwise | E2E | covered |
| A zero-outcome record is held, not paid as zero | [#26](../../issues/26) | `TestAZeroOutcomeIsHeldWithAReasonRatherThanPaidAsZero` — held with an explanation and an owner rather than RELEASED for nothing | E2E | covered |
| No raw national identifier reaches the unclear queue | [#22](../../issues/22) | `TestAnUnmatchedNationalIdentifierIsNeverStoredRaw` — the queue keeps the salted hash so a row can still be re-attributed. Found by review: the fixtures all joined on a phone, so this path was never exercised | E2E | covered |
| Canonicalisation matches RFC 8785 | [#16](../../issues/16) | `pkg/credential` — `&`, `<` and `>` are not escaped. Found by review: Go's `json.Marshal` escapes them, both sides of CREST agreed, and no conformant outside verifier would have | Unit | covered |
| Auto-confirmation happens with nobody asking | [#23](../../issues/23) | `TestScheduledSweepPaysWithNobodyAsking` via `make test-e2e-sweep` — the clock passes T=7 and the window exits `auto` and releases payment with no `POST /v1/sweep` anywhere in the test. Every other T=7 scenario posts the sweep by hand, which proves the logic and not the wiring; on a deployment nobody posts it at all, so before this the auto exit — the only one of the four no person triggers — never fired. Negative control run: with `SWEEP_EVERY=0` the same test fails "still open" | E2E | covered |
| W3 — silence is not consent | [#23](../../issues/23) | `TestSilenceStillPaysAndStaysDisputable` — seven days advance, the sweep auto-confirms, payment is released, and the claim is still disputable afterwards | E2E | covered |
| Every T=7 exit releases payment | [#23](../../issues/23), [#26](../../issues/26) | `TestADisputeStillReleasesPayment` for the hardest exit, plus `/v1/unreleased` asserted empty. All four routes are now exercised — self, auto, dispute, and supervisor-assisted via the unreached path | E2E | covered |
| W5 — a disputed claim never destroys the unit | [#22](../../issues/22) | The unit is fetched after its claim is disputed and is unchanged; `Contest.target` cannot name a unit at all | E2E | covered |
| W1 — an unattributable row goes to the unclear queue | [#22](../../issues/22) | `TestAnUnattributableRowGoesToTheUnclearQueue` — the row is queued with a reason and a row reference, never guessed at and never dropped | E2E | covered |
| An unattributed row can be given to the worker who did the work | [#25](../../issues/25) | `TestAnUnattributedRowBecomesTheClaimOfTheWorkerWhoDidTheWork` — the row becomes that worker's claim, their confirmation window opens, the row leaves the queue and the resolution names who decided. Until this landed the queue could be listed and never worked, so every row in it was somebody who did the work and was not going to be paid | E2E | covered |
| Work resolved weeks late still gets its full seven days | [#25](../../issues/25) | `TestWorkResolvedWeeksLateStillGetsItsFullSevenDays` — the clock is advanced 20 days before resolution and the window still closes seven days after it. Starting at ingest would auto-confirm a record the worker had never been shown | E2E | covered |
| A row that failed the evidence contract cannot be attached to a person | [#25](../../issues/25) | `TestARowThatFailedTheContractCannotBeGivenToAWorker` — only a row whose `kind` is `unattributed` resolves; the others would launder a failed record into the ledger wearing a worker's name | E2E | covered |
| Submitting evidence does not let you decide whose work it was | [#25](../../issues/25) | `TestSubmittingEvidenceDoesNotLetYouDecideWhoseWorkItWas` — `resolve-unclear-evidence` is a separate grant from `submit-work-evidence`, and the refused attempt leaves the row workable | E2E | covered |
| The same unattributed work cannot be resolved twice | [#25](../../issues/25) | `TestARowCannotBeResolvedTwice` — the second attempt is refused rather than deduplicated afterwards, and the row is locked `FOR UPDATE` so two people working the queue cannot both create a claim | E2E | covered |
| Resolution honours a withdrawn enrolment consent | [#25](../../issues/25) | `TestARowCannotBeResolvedOntoAWorkerWhoWithdrewConsent` — §9's rule applied at the second door into the ledger, not only at ingestion | E2E | covered |
| An unknown consent capture method is a 400, not a 500 | [#25](../../issues/25) | The allowed set lived only in migration 0005's CHECK, so a typo came back as a constraint violation the caller could not distinguish from an outage. Found while writing the resolution scenarios | E2E | covered |
| W7 — probable matches hold | [#20](../../issues/20) | The registry answers 409 and writes a hold row, exercised end to end: two parties are created on one phone number and the resolve is asserted to be a 409 with a hold, not a pick. The gap this row used to record — no fixture had two parties sharing an identifier — is closed by `twoPartiesOneNumber` | E2E | covered |
| A merge without the worker's confirmation cannot be expressed | [#20](../../issues/20) | `TestAMergeWithoutTheWorkersConfirmationCannotBeExpressed` — refused by the handler and by a CHECK constraint, so a future code path that forgets still cannot write the row (§4) | E2E | covered |
| `merges_without_confirmation` is a query, not an assertion | [#20](../../issues/20) | `GET /v1/holds/metrics`, asserted zero after a refused merge. **It read zero before this landed because merging was impossible** — a control that holds because the action does not exist has not been tested, it has been avoided | E2E | covered |
| A confirmed merge makes the identifier resolve to the survivor | [#20](../../issues/20) | `TestAConfirmedMergeMakesTheIdentifierResolveToTheSurvivor` — the absorbed party is not deleted and points at the survivor via `mergedInto`, so a worker's history keeps its shape across the correction; the next batch resolves rather than re-raising the same hold | E2E | covered |
| Two people sharing a phone are not merged | [#20](../../issues/20) | `TestTwoPeopleSharingAPhoneAreNotMerged` — one handset per household is ordinary; the identifier comes off the other candidate and nothing else about them changes | E2E | covered |
| A hold is closed by choosing among its own candidates | [#20](../../issues/20) | `TestAHoldCannotBeResolvedOntoSomebodyElse` — naming a third party is a 409, not an attribution | E2E | covered |
| The hold queue does not disclose the colliding identifier | [#20](../../issues/20) | `TestTheHoldQueueDoesNotHandOutTheCollidingIdentifier` — §16 asks for existence, not content, and the content here is the phone number of every worker two records disagree about | E2E | covered |
| The e2e suite passes twice against one stack | [#79](../../issues/79) | Two consecutive `make e2e-run` invocations against a stack left up. Found while adding the hold scenarios: two consent-withdrawal tests withdrew on behalf of fixture Workers A and B, and a `[:8]` slice meant a test's "unique" identifier hash was the constant `shared-i`. Eight unrelated tests failed on the second run, all of them looking like product defects | E2E | covered |
| A re-assessed source downgrades without reissuance | [#27](../../issues/27) | `TestReassessingASourceDowngradesAnUnchangedCredential` — the same credential bytes verify at a lower tier, and the verdict says why. Lifting the assessment restores it | E2E | covered |
| Withdrawal is visible to a verifier | [#27](../../issues/27) | `TestARevokedCredentialStopsVerifying` | E2E | covered |
| The tier is never stored | [#5](../../issues/5), [#27](../../issues/27) | The credential is asserted to carry no `tier` field; the verdict computes one per request | E2E | covered |
| W6 — verifiable without CREST | [#27](../../issues/27) | **Not proven.** `verification` resolves the issuer key, the status list and the definition over HTTP. The credential now carries `evidenceFields` so a tier map can be evaluated offline, but no offline verifier exists and the printed-card path (#1) is untouched | E2E | planned |
| W10 — a held payment has a reason and an owner | [#26](../../issues/26) | A CHECK constraint makes a hold without an owner unrepresentable, and `/v1/reconciliation` reports gaps that have no reason. **No scenario yet forces a hold** | E2E | partial |
| The outbox survives a crash between a state change and its side effect | [#47](../../issues/47) | `harness/scenarios/outbox_test.go`, reshaped by #129: the exit and the instruction are one service now, so the deterministic two-service staging is gone. `TestAPaymentReleaseSurvivesThePaymentsServiceBeingKilled` confirms, **SIGKILLs** the payments service straight after the exit commits, restarts it, and asserts the release lands exactly once — the kill races the in-process relay, and both interleavings prove nothing lost, nothing doubled | E2E | covered |
| A consent artefact round-trips and can be withdrawn | [#47](../../issues/47) | `harness/scenarios/blobs_test.go` against the SeaweedFS in compose: stored, read back byte-identical, deleted, and a withdrawn artefact reads `ErrNotFound`. Deleting twice is not an error | E2E | covered |
| The object store authenticates | [#47](../../issues/47) | A client with the wrong secret is refused. Without this every signing test is decorative — a broken signature would pass by being ignored | E2E | covered |
| SigV4 is right, not merely self-consistent | [#47](../../issues/47) | `pkg/store/s3_test.go` reproduces AWS's published "PUT Object" example signature byte for byte. The lesson of C19: signing and verifying with the same code proves only that the two halves agree | Unit | covered |
| An artefact key discloses nothing | [#47](../../issues/47) | Keys are minted by the store, never supplied — `Put` takes a kind, not a key, so a caller cannot write `consent/<phone>.ogg`. 500 mints, all unique and opaque; a kind containing a path separator is refused | Unit | covered |
| An oversized artefact is refused, not truncated | [#47](../../issues/47) | A part-stored consent recording stops before the person says what they agreed to, and is indistinguishable from a complete one | Unit | covered |
| Every party-acting endpoint checks its caller | [#102](../../issues/102) | The whole e2e suite now runs authenticated: every scenario logs in as the party it acts as (or their permitted actor), the impersonation refusals of #89 extend across the surface, and the suite passed twice consecutively against one stack. Deliberately open surfaces carry in-code comments saying why; services call `/internal/*` twins per the §16 service-identity ruling | E2E | covered |
| A first login is not held to a cached "you do not exist" | [#102](../../issues/102) | The binding endpoint's self-proof is possession of the token whose subject is being bound, and a landed binding invalidates the middleware's cache entry (`identity.Forget`) — exercised by every scenario's login, since each one binds then immediately acts | E2E | covered |
| Self-proof binds only the unbound or the already-bound | [#102](../../issues/102) | `TestAStrangersOwnTokenCannotClaimAnAlreadyBoundParty`, `TestTheFirstBindingOnAnUnboundPartySelfProves`, `TestReBindingTheSameSubjectStaysSelfProved` (unit, on `selfBindAccepted`); end to end, every harness login exercises the rule — the fixture workers arrive already bound to eSignet/OTP subjects, so their logins go through the supervisor's act-for-party grant, the door a real assisted enrolment uses. The residual — an unbound ULID is a claim capability until enrolment binds it — is written down in `bindings.go`, stated in §4, and tracked as [finding #123](../../issues/123) | Unit + E2E | covered |
| Authorizations are a signed-in surface | [#102](../../issues/102) | `TestAnonymousCallersCannotMintOrListAuthorizations` (E2E): anonymous `POST /v1/authorizations` and `GET /v1/authorizations` both 401. The seeder and every granting scenario authenticate; the issuance path's best-effort read (in verification since #137) moved to the `/internal/authorizations` service twin (§16), because a refused read there silently drops the credential's grant references — the worst failure shape, a credential that looks issued and cannot be walked | E2E | covered |
| CORS is chosen, never defaulted | [#102](../../issues/102) | `TestNoOriginsMeansNoCORSHeadersNotAWildcard` and `TestOnlyNamedOriginsAreEchoedAndPreflightIsAnswered` (unit): unconfigured means no headers at all, and only named origins are echoed — tokens ride these requests | Unit | covered |

## Registry and definitions

| Feature | Issue | How it is proven | Layer | Status |
|---|---|---|---|---|
| Org self-registration + terms + approval | [#20](../../issues/20) | `TestAnOrganisationCannotApproveItself` and `TestAnOrganisationCannotBeApprovedBeforeAcceptingTerms` (E2E), plus a CHECK constraint that refuses a self-granted approval independently of the code | E2E | covered |
| Assisted enrolment — a worker with no phone | [#20](../../issues/20) | `TestAWorkerWithNoPhoneCanStillBeEnrolled`: the enrolment succeeds, the enroller is recorded and readable, and the worker's derived assurance is **not** raised by having been vouched for | E2E | covered |
| Public/private placement rule | [#20](../../issues/20) | `TestOrganisationFaceCarriesNoContactOrIdentityData` asserts on the serialised document, not the map keys, so a nested field cannot pass; `TestAWorkerNeverReachesTheRegistrySubstrate` (E2E) checks the publication surface holds no worker | Unit + E2E | covered |
| Only organisations' authorizations are published | [#20](../../issues/20), [finding #68](../../issues/68) | `TestAuthorizationFaceRefusesAWorkersAuthorization`. §3 says authorizations are public without qualifying which; a public log of workers' authorizations is a permanent roster of who works where | Unit | covered |
| Coverage-by-place (g4_4) is honest counts, never a fabricated percentage | — | `GET /v1/coverage?attribute=<key>` groups registered, non-merged parties by a self-declared `Party.Attributes` key the deployment names — CREST has no built-in geography vocabulary, and a population denominator is deployment L2 data it does not own, so `denominator.<place>=<n>` query parameters are the read's only source of one and are never stored. `TestCoverageByPlaceCountsEachPlaceOnce`, `TestCoverageByPlaceSurfacesUnspecifiedRatherThanDropping`, `TestCoverageByPlaceJoinsADenominatorOnlyWhenOneWasGiven`, `TestCoverageByPlaceHandlesNoPartiesAtAll`, `TestCoverageByPlaceOmitsCoverageForAZeroDenominator` cover the aggregation as a pure function | Unit | spike |
| Registry quality worklist (g4_5) names gaps, never scores completeness | — | `GET /v1/quality-worklist` joins three already-typed facts per party — an identity binding present or not, an enrolment consent granted or not, an open match hold naming the party or not — into a row per gap, dropping any party with none. No new quality score. `TestQualityGapsNamesEveryMissingThing`, `TestQualityGapsIsEmptyForACleanParty`, `TestQualityGapsIsPartial`, `TestBuildWorklistDropsPartiesWithNoGap`, `TestBuildWorklistIsEmptyForAnEmptyRegistry`, `TestBuildWorklistTreatsAMissingInputAsUncomputedNotClean` | Unit | spike |
| A deployment says who it is, resolvably | [#70](../../issues/70) | `TestADeploymentSaysWhoItIsAndAVerifierCanResolveIt` (E2E): `GET /v1/instance` is unauthenticated, names the operator Party and the publisher key id, and its own publication row agrees with what it claims. Verified on the deployed node — `crest/instances/crest:instance:local` v1, leaf 13 of 14, root signature checked | E2E + spike | covered |
| A deployment refuses to invent its own identity | [#70](../../issues/70) | `TestLoadInstanceRefusesToInventAnIdentity` and `TestLoadInstanceDefaultsOnlyLocally`. Two deployments that both defaulted would collide in one namespace, and on an append-only log that is not a mistake anyone can take back | Unit | covered |
| Publication is idempotent by content, not existence | [#70](../../issues/70) | `TestAnUnchangedIdentityHashesTheSame`, which also asserts a **rotated publisher key does** change the digest. "Publish if absent" would freeze the first answer forever, so the log would go on advertising a key that no longer signs anything | Unit | covered |
| An external institution's class is who they are, not how they integrated | [G1 #8](../../issues/8) | Two vectors in `pkg/strength/testdata/vectors.json`: the same `institutional-system` source earns tier 3 for a `system-of-record` and tier 1 hand-keyed. The transport it arrived over is asserted irrelevant by `TestTransportDoesNotAffectStrength` | Unit | covered |
| The skill list is published and immutable | [#16](../../issues/16) | `TestASkillCodeIsPublishedAndImmutable` (E2E): republishing a code under a new meaning is refused, superseding a code that does not exist is refused, and the code reaches the registry. Verified on the deployed node — `crest/skills/CREST-SKILL:chw.bednet-distribution.v2` v1, leaf 17 of 18, root signature checked. The version lives inside the code, so an edit is never a correction — the code is already in credentials that cannot be rewritten | E2E + spike | covered |
| A skill code's supersession chain is published | [#16](../../issues/16) | `TestSkillFaceCarriesTheSupersessionChain`. Without it a worker holding a credential against v1 has a code that resolves and no way to learn it has a successor — their older evidence silently stops connecting to the current vocabulary | Unit | covered |
| A skill code must carry its own version | [#16](../../issues/16) | `TestSkillCodeMustCarryItsVersion`. A code without a version is one whose meaning can change under the credentials already carrying it | Unit | covered |
| Duplicate hold queue | [#20](../../issues/20) | **`merges_without_confirmation = 0`** — probable match holds, never auto-merges. The 409 path still has no fixture with two parties sharing an identifier (see the W7 row) | Unit + E2E | partial |
| DeDi abstraction / Postgres fallback | [#20](../../issues/20) | `pkg/dedi` behind one `Publisher` interface. The whole E2E suite runs on the fallback in CI; the node path is proven on demand against the deployed node (`make verify-registry`). Not "the same suite against both backends" — the two are deliberately *not* equivalent, and `Receipt.Transparent` is how a caller sees the difference | Contract + E2E | partial |
| DeDi's publisher signature, re-implemented | [#20](../../issues/20) | `TestPreimageWireContract` pins the exact preimage bytes against vectors generated from DeDi-node's own `internal/publisher`, so a drift in either implementation fails at `make test-unit` rather than as "signature does not verify" in a deployment | Unit | covered |
| A signed registry write uses wall time, not the domain clock | [#20](../../issues/20) | `TestSigningTimestampIsWallTimeNotTheDomainClock`. Found the hard way: services run on a driveable clock at the fixture epoch, and signing with it made every write to the deployed node fail on replay protection | Unit | covered |
| Author ≠ approver on definitions | [#21](../../issues/21) | Self-approval rejected in code and in a CHECK constraint; the harness seeds through DRAFT → ratify → activate on every run | Unit + E2E | covered |
| Definition immutable once ACTIVE | [#21](../../issues/21) | Edit rejected; a new version is the only way to change one; old versions still resolve | Unit + E2E | covered |
| An ACTIVE definition is resolvable outside CREST | [#21](../../issues/21) | Publication is enqueued in the same transaction as the ACTIVE transition, so a crash cannot leave a definition credentials pin and no verifier can resolve. `TestAnActivatedDefinitionIsResolvableOutsideCREST` (E2E) checks the publication exists; the deployed node was checked by hand with `make verify-registry`, and an inclusion proof validated by `tools/spikes/dediproof` — a second implementation | E2E + spike | partial |
| A pinned registry lookup returns the version it pinned | [#20](../../issues/20) | `TestResolveRefusesWhenThePinIsIgnored`. DeDi ignores an unrecognised query parameter and answers with the latest version and a valid proof (spike finding 3), so `pkg/dedi` compares what came back with what was asked for and refuses | Unit | covered |
| Review-by dates: overdue flags, never lapses | [#105](../../issues/105) | `TestAnOverdueAuthorizationStillPermitsAndIsListedForReview` (E2E): an authorization past `reviewBy` still permits, says `overdue` on the permits answer, still accepts a submission, **and** appears on `GET /v1/authorizations/overdue`. Both halves in one scenario, because "keeps working" alone is the rejected ruling and "is listed" alone is a lapse wearing a flag | E2E | covered |
| Grant narrowing binds at submission | [#105](../../issues/105) | `TestARevokedGrantStopsNewWorkButNotWorkInFlight` (E2E): work submitted, grant revoked; the in-flight claim confirms and pays, the next submission is refused. Each half alone passes for the wrong reason — the first because nothing re-checks anywhere, the second because nothing was granted | E2E | covered |
| Definition authoring: a draft compiles to a valid definition, or names its open questions | P-3 wave | `TestCompileCompleteDraftValidates` (the compiled result passes the definition schema, lands in DRAFT, carries counting and classification) and `TestCompileNamesEveryOpenSection` (an empty draft yields a named problem per section — p3_18's list, not a refusal). Compile is one pure function; validate and submit run the same one, so the review can never disagree with the submission | Unit | partial |
| The two faces of one record cannot disagree on tier | P-3 wave | `TestCompileRefusesTierAboveCeiling`: a tier-map rule granting above the worker face's ceiling is refused by name at compile, because faces are projections of one record (§7) | Unit | covered |
| Connection details never carry a secret | P-3 wave | `TestCompileRefusesSecretsInConnections`: settings keys matching password/token/api-key/secret/auth shapes are refused; CREST stores a `credentialRef` naming where the platform team keeps the secret (p3_26). Broad by design — a false positive costs a rename, a false negative persists a credential CREST promised never to see | Unit | covered |
| Extensions are typed, and the types are checked | P-3 wave | `TestCompileChecksExtensionTypes`: number/boolean/date values that do not read as their declared type are refused per key; well-typed ones pass | Unit | covered |
| Ratify-with-pending-fields is the ratifier's own declaration | P-3 wave | `TestApplyRatification` (separation of duties still refuses self-ratification; pending fields recorded trimmed and deduplicated) and `TestRatificationWithoutPendingLeavesFieldAbsent` (no declaration is absence, not an empty list). The declaration is also appended to the version's event record in the same transaction as the state change — the two-records-one-signature flow (p3_16) — but the transaction itself is not yet under test | Unit | partial |
| The wizard writes the definition, and the review cannot disagree with it | P-3 wave | The Playwright walk "the authoring wizard writes a definition, proves it dry, and has it ratified with its gaps named" drives all 25 reference wizard screens as the author persona against one real draft: the counting fork writes `counting.basis` before it navigates, every Continue is a real per-section `PUT`, and `#/define/open` renders the **validate endpoint's own** open-questions list — asserted non-empty on a one-section draft and empty when the draft is ready, because it is the same `compile()` the submit runs | Playwright (`make e2e-apps`) | covered |
| A source class caps the tier, and the cap is shown as derived | P-3 wave | `#/define/source` (p3_22) computes the strongest tier the draft's own map admits for the chosen `sourceClass`/`captureMethod`, labels it "derived, not stored", and shows the per-rule admit/refuse table it came from. The walk asserts a programme system's digital capture reaches Tier 2 and not the Tier 3 the map's top rule would grant to a national system. The authoritative judgement is the strength function, and the dry-run step asserts it agrees | Playwright (`make e2e-apps`) | covered |
| The console cannot express a source credential | P-3 wave | `#/define/connect` (p3_26) has no password input at all — the walk asserts `input[type=password]` count is zero — and takes only a `credentialRef`. A setting key named `apiKey` is refused by the service in the same words at validate as at submit, the refusal is rendered on the screen, **the wizard does not advance**, and the key is then removable so the refusal is a correction rather than a dead end. On successful submit the connection and mapping become a `source-binding` LinkedRecord pinned to the resulting definition version; the walk reads it back and asserts the version, system, and type, proving p3_24–p3_28 did not vanish with the draft | Playwright (`make e2e-apps`) | covered |
| The adaptor library is told honestly | P-3 wave | `#/define/adaptors` (p3_24) renders `GET /v1/adaptors` verbatim: `csv-batch@1` is selectable, every other class is rendered in the "not available" posture carrying the service's own `not-implemented` note, and the reference's own primary button ("Use DIGIT HCM") names a class CREST does not have, so it carries an on-screen gap note instead of pretending to choose it. Asserted in the walk | Playwright (`make e2e-apps`) | covered |
| Version immutability protects claims already made | P-3 wave | The Playwright walk "a clone submits as the next version, and ratifying names nothing pending" clones the seeded definition, submits it as **v2 of the same definition**, and then reads v1 back from the service: still `version: 1`, still `ACTIVE`, and without the classification the clone added. This is the property that makes a credential pinned to v1 meaningful after v2 exists — a submit appends, it never rewrites | Playwright (`make e2e-apps`) | covered |
| Ratification with pending fields, and without, are different records on screen | P-3 wave | The first walk ratifies naming a real pending field (nothing prices the unit yet) plus one in the ratifier's own words, and asserts both on p3_16 together with "the ratifier named these, not the author". The second walk ratifies naming nothing and asserts the "nothing pending" state instead, with no pending-field rows. Absence is not an empty list, and the two are distinguishable on the screen as well as in the record | Playwright (`make e2e-apps`) | covered |
| Two records, one signature — the event trail names every actor | P-3 wave | p3_16 (`#/ratified`) reads `GET /v1/definitions/{id}/events` for real and the walk asserts SUBMITTED, RATIFIED and ACTIVATED rows are all present for the version, that the submission's actor is the author party and the signature's actor is the approver party (two different parties), and that a PAYMENT_HANDOFF row appears once pricing is handed on. `POST .../ratify` with `publish:true` now performs the DRAFT→RATIFIED→ACTIVE transitions, appends both governance events, and enqueues the durable publication message in one database transaction — the UI no longer composes two HTTP calls while claiming one act | Playwright (`make e2e-apps`) + definitions unit suite | covered |
| Author and approver are two parties, not two menus | P-3 wave | The console's author persona signs in as the fixture world's specifier party and the approver as the organisation, so the definitions service's self-ratification refusal is load-bearing rather than assumed. Three independent mechanisms, each asserted: the navigation carries no entry across the boundary, the route guard bounces every hand-typed wizard route for the approver and `#/ratify` for the author ("the approver cannot reach the author's wizard", "the author cannot reach the approver's signature"), and the service refuses the signature. p3_15 also disables its own sign button and states the refusal when the reader is the author | Playwright (`make e2e-apps`) | covered |
| Pricing is handed off, never assumed | P-3 wave | p3_pay (`#/handoff`) posts a real `payment-handoff` linked record and the walk asserts the definition reads "unpriced" before and "handed off, awaiting a rate" after, that the handoff row names inviter and invitee, and that the screen states the invitation composes with F-1's `RateOwnerAssignment` rather than substituting for it. What is **not** proven here is the other side: no test carries the invitation into the rate owner's own session | Playwright (`make e2e-apps`) | partial |
| The document under the form is the real one | P-3 wave | p3_19 (`#/define/anatomy`) renders the stored version's own JSON, and the walk asserts it carries `"version": 1`, `"state": "DRAFT"`, the author party and the unit of work — and that the institution extension (`mgnrega.contractorRef`) appears in the second layer and **not** in the base primitive, which is the whole claim the screen makes about the two layers being separate | Playwright (`make e2e-apps`) | covered |
| Cloning edits a copy, never the source version | P-3 wave | `TestCloneRoundTrip`: a wizard document rebuilt from a compiled definition compiles back to the identical definition (byte-compared), and nothing on the clone path writes to the source. `TestCompileRefusesSelfCascade` refuses a definition as its own training prerequisite | Unit | covered |
| The spreadsheet template is derived from the version, never stored | P-3 wave | `TestTemplateIsPinnedToTheVersion` (two versions with different required fields produce different templates, filenames name the version) and `TestTemplateColumns` (canonical columns present, definition requirements deduplicated). `GET /v1/definition-drafts/{id}/template` derives the file for `baseVersion+1` mid-wizard without minting a definition; after submit the immutable-version endpoint derives the same columns again — p3_23 occurs in the reference's actual order instead of being deferred until after submission | Unit + Playwright (`make e2e-apps`) | covered |
| Dry run judges sample rows the way ingestion would, committing nothing | P-3 wave | `TestPresentAndMissing` covers the field judgement; the endpoint reuses the CSV adapter's `Parse` and `strength.Evaluate` verbatim (both carry their own suites) and performs no DB write by construction. Driven for real in the P-3 Playwright walk: sample CSV rows go through `#/define/dryrun`, the screen renders a derived tier per row with its reasons, and `committed: false` plus the endpoint's own "no unit, no source, no queue entry" note are asserted on the page. **The derivation is proven rather than asserted**: the identical rows are re-judged with `sourceClass` changed from `programme-system` to `self-reported` and come back Tier 1 instead of Tier 2, then back again — an answer that changes with the provenance and not the rows cannot have been stored on them. The handler's own refusals (missing source facts, an untestable draft) still have no test | Unit + Playwright (`make e2e-apps`) | partial |
| Draft lifecycle: mutable only while OPEN, submit is one transaction | P-3 wave | The state check and write are single SQL statements (`updateDraftDoc`, `closeDraft`), and submit writes the definitions row, implied LinkedRecords, the SUBMITTED event and the draft closure in one transaction. Now driven end to end: the Playwright walk `tests/e2e-apps/apps.spec.js` "the authoring wizard writes a definition, proves it dry, and has it ratified with its gaps named" creates a real draft, writes all nine sections through the console's own Continue buttons, submits, and then **asserts the closed draft refuses a further section write** (409 `draft_closed`, surfaced on the screen and in the error bar). The store layer still has no unit fixture, and no test kills the process mid-submit, so the atomicity of the transaction itself is argued from the code rather than proven | Playwright (`make e2e-apps`) | partial |
| A recovery needs two voices from distinct authorities | [#106](../../issues/106) | `TestTwoVoicesFromDistinctAuthoritiesRecoverAWorker` (E2E): self-confirmation 400, unvouched confirmer 403, second same-authority voice 409 — refused by a UNIQUE constraint, not a handler — second distinct voice confirms, completion binds the new subject and it resolves to the worker | E2E | covered |
| A recovered worker derives IA-1 until re-anchored | [#106](../../issues/106) | The same scenario asserts the derived assurance after completion. Vouching is community knowledge, not a national identity check; re-anchoring wins automatically because assurance is derived, never stored | E2E | covered |
| An override without a reason cannot be expressed | [#106](../../issues/106) | `TestAnOverrideWithoutAReasonCannotBeExpressed` (E2E) plus the `overrides_have_a_reason_and_an_owner` CHECK constraint: no reason 400, no `override-recovery` function 403, the worker's **own supervisor** 403 even holding the function (G1 #10), the override readable afterwards, and overdue after the review date on `GET /v1/recoveries?overdue=true` | E2E | covered |
| Recovery-contact nomination is routing, never a phone number | [#106](../../issues/106) | w1_7: `POST /v1/parties/{id}/recovery-contacts` stores a party-linked nomination — never a raw number-as-identity — with `nominationAdmissible` unit-tested (`TestANominationNamesSomebodyElse`: nobody named, self-nomination). One live nomination per pair and contact≠worker are table constraints (0014). Nomination never widens who may confirm (routing, not eligibility — blueprint §16 sub-ruling 2026-09-03). Handler + constraints now driven end to end by the #190 recovery walk: nominate, revoke-keeps-the-row, re-nominate, self-nomination refused loudly through the screen | Unit + `make e2e-apps` ("recovery: nomination routes, a refusal is owned, and two authorities confirm") | covered |
| The confirmer's own pending view | [#106](../../issues/106) | w4_1/w4_2: `GET /v1/recoveries?confirmerPartyId=` answers open recoveries of workers who nominated the caller, to the caller alone. SMS delivery is honestly absent — a channel adapter gap held with the notify decision (#150); this is the pull half the adapter would deliver from. The #190 recovery walk does exactly what this row asked for: nominates, opens a recovery as the custodian, and reads the view as the signed-in confirmer (the SMS gap asserted on-screen) | `make e2e-apps` ("recovery: nomination routes, a refusal is owned, and two authorities confirm") | covered |
| A recovery refusal is a recorded answer with an owner, not a dead end | [#106](../../issues/106) | w4_3: `POST /v1/recoveries/{id}/refusals` — `refusalAdmissible` unit-tested with its refusals (`TestARefusalIsARecordedAnswerNotADeadEnd`: no reason, self-refusal, every decided state), reason required by CHECK constraint too, one refusal per person by PK, recovery stays OPEN, `?refused=true` is the attention queue with the opener as owner. Handler now driven end to end by the #190 walk: refusal-without-reason refused, with-reason recorded, recovery held OPEN with the opener named, and the same voice's later YES still counts | Unit + `make e2e-apps` ("recovery: nomination routes, a refusal is owned, and two authorities confirm") | covered |
| No-document enrolment is a confidence check, on the record | [#20](../../issues/20) | w1_4: `confidence-check` is a named assisted-enrolment method (`validEnrolmentMethod`, `TestConfidenceCheckIsANamedEnrolmentMethod`) — a provenance fact, not a new binding class: assurance stays derived from `identityBindings` (IA-0 until a route verifies or an anchor binds), so the existing retroactive-upgrade tests carry it. Which questions make a confidence check is L2. Driven end to end by the #190 walk: the field door's `#/confidence` posts the method through the real handler (whose CHECK constraint #190 had to widen) and reads it back from `GET /v1/parties/{id}/enrolment` | Unit + `make e2e-apps` ("assisted enrolment: the confidence check records a method, never a tier") | covered |
| Qualification arrival re-derives everything already earned | [#17](../../issues/17) | w1_17 needed **no new code** — the ruled design already does it: `POST /v1/parties/{id}/identity-bindings` appends, assurance derives (`assuranceOf`), verify-time strength re-evaluates. Proven by `TestAssuranceImprovingLaterRaisesTheTierWithNoReissuance` (unit, pkg/strength) and `TestBindingAnAnchorLaterRaisesAssuranceWithNothingReissued` (unit, parties) | Unit | covered |
| Definitions name fields the deployment owns, not the source | [#56](../../issues/56) | `TestAMappedEnrichmentColumnArrivesUnderItsCanonicalName` (unit): a source spelling "Household ID" arrives under the configured canonical name and its own spelling is gone — because the failure this prevents is a tier rule that never matches and says nothing. Mapping is per-deployment L2 (`Mapping.Enrichment`), decided over per-source definitions | Unit | covered |
| Worker message wording is deployment configuration | [#59](../../issues/59) | `TestTheDefaultTemplatesRenderTheOriginalSentence`, `TestATranslatedSetRendersWithItsOwnKeywords`, and the one #59 demanded: `TestATemplateWithoutThePaidEitherWaySentenceIsRefused` — a duress-shaped template fails at startup, in any language, because the paid-either-way sentence is W4's, not style. Keywords live in the same config as the prompt | Unit | covered |
| A CBOR-wrapped PixelPass card decodes to its JSON | [#92](../../issues/92) | `TestACardFromTheirLibraryHoldingJSONDecodesToTheJSON` against a vector from MOSIP's own library (the C19 lesson: a codec agreeing with itself proves nothing), `TestTheCBORSubsetCoversWhatJSONCanHold` including the first integer a float64 loses, and `TestCBOROutsideTheJSONSubsetIsRefusedByName`, and `TestNonFiniteFloatsAreRefusedAsOutsideTheJSONSubset` (`cbor_test.go`) — NaN and the infinities, which CBOR can spell and JSON cannot, are refused by name at decode rather than surfacing later as an opaque `json.Marshal` error; `TestAFiniteFloatStillDecodes` pins the boundary — the silent-success failure shape is now impossible | Unit | covered |
| Projects, on the Context primitive | [#172](../../issues/172) | `TestNamingAConfiguratorIsAProposalNotAnAssignment` plus schema validation of the whole document (`TestTheSchemaRefusesAMalformedHandover`). A project is a `Context` (§2: project and campaign are what profiles call it), so there is no new primitive and no second store — `POST/GET /v1/projects` is a face over it. Coverage rides `Context.configuration`, opaque. **Not yet proven end to end**: no E2E scenario drives `p1_3` against real services, and no console surface consumes these routes (Step 3) | Unit | partial |
| The handover is acknowledged, not assumed (design finding F2) | [#172](../../issues/172) | `TestOwnershipDecisions` — seven cases, six of them refusals: accept, decline with a reason, decline with none, decline with whitespace for a reason, an answer from somebody other than the named party, an answer to a handover nobody made, and a second answer to a settled one. Plus `TestDecliningDestroysNothingAndNamesWhoDeclined`, which asserts the project, its state and its configuration are untouched and the declining party stays named | Unit | covered |
| A decline returns the project to the queue and deletes nothing | [#172](../../issues/172) | The test above, and `TestReHandoverAfterADeclineReturnsToPending` / `TestReNamingAnAcceptedProjectStillNeedsANewAcknowledgement`. The trail is append-only (`context_ownership_events`) because a re-handover sets the current view back to PENDING and "who declined this, and why" is exactly the fact that would otherwise be gone. **The trail's append is proven only by the unit tests' event values**, not yet by a database-level test of the sequence under concurrency | Unit | partial |
| A proposal does not carry authority (found by review) | [#172](../../issues/172) | `TestOnlyAnAcknowledgedConfiguratorMayWrite` — six cases over the two pure gate predicates: the accepted configurator writes, a merely named one reads but does not, a declined one reads but does not, a stranger is neither, an unhanded project names nobody, and an empty caller never matches an empty party (the case that matters on a stack with authentication switched off). Found by the correctness reviewer: the write gate originally compared party ids and never read the state, so a party who had **declined** kept write authority over the project's finance link, support owner, gates and partner grants — writes that outlive the refusal, because a re-handover cannot un-grant what the previous nominee did. **The routing of each handler to the read gate or the write gate is not itself tested**; the predicates are | Unit | partial |
| Activation refuses with its conditions named | [#172](../../issues/172) | `TestActivation` — six cases: every condition satisfied, an unacknowledged handover, a declined handover, an unsatisfied gate, an already-ACTIVE project (idempotent), and a CLOSED one. Every refusal is asserted to carry the conditions, because "cannot activate" without saying what is missing is the dead end this system does not use. `TestActivationConditionsAreReadableBeforeAnythingIsAttempted` covers the read | Unit | covered |
| Gate names are L2; satisfaction is not the caller's to assert | [#172](../../issues/172) | `TestDeclaringGatesKeepsSatisfactionsAndRefusesNonsense` (re-declaring keeps an answer, a dropped gate does not resurrect, duplicates and unnamed gates refused, an undeclared gate cannot be satisfied) and `TestSatisfyingAGateTwiceKeepsTheFirstAnswer` — when a condition was met is a fact, and re-asserting it must not move the date. `TestAProjectWithNoDeclaredConditionsActivates` pins the deliberate case, so changing it is a decision rather than a drift | Unit | covered |
| A role is held, not just recorded | [#172](../../issues/172) | No new storage was needed: `Authorization` already carried `approvedByPartyId` and `approvedAt`, and `GET /v1/projects/{id}/roles` and `GET /v1/organisations/{id}/roles` read them back with the holder, the state and `grantableBy` — so a guard can name somebody who can grant the role rather than refusing into a dead end. **Unproven**: the listing handlers have no test of their own; they are read paths over `contextGrants`/`authorityGrants`, and the SQL is exercised by nothing yet | — | unproven |
| A partner grant is narrower than the terms, and it ends | [#172](../../issues/172) | `TestAGrantCannotExceedTheTermsTheOrganisationAccepted` — six cases including the empty set, every uncovered function named rather than just the first, and case-sensitivity (a permission string is a published identifier, so "nearly" is not a match). The end date and the APPROVED-organisation gate are enforced in the handler and **not yet tested**: both are refusals with no test behind them | Unit | partial |
| The partner directory reads decisions somebody else already made | [#172](../../issues/172) | `TestDirectoryFilters` (nine cases: no filter, sector, sector case, wrong sector, country, wrong country, a covered permission, an uncovered one, and one of two covered) and `TestAnUnstatedAttributeIsAbsentRatherThanGuessed` — an organisation that never stated a sector must not match a sector filter. The APPROVED-only join is SQL and **unproven by test**. The public organisation face is untouched, which `TestOrganisationFaceDoesNotCarryAttributes` already guards | Unit | partial |
| CREST does not invent account codes | [#172](../../issues/172) | `TestAFinanceLinkIsAReferenceToSomebodyElsesCode` — five cases: a system and a code accepted, a code with no system refused (it cannot be traced back to whoever minted it), a system with no code refused, nothing refused, and an unreasonable length refused. There is no generator, no format check and no sequence anywhere near the record, which is the actual claim | Unit | covered |
| Support ownership always names somebody | [#172](../../issues/172) | `TestSupportOwnershipAlwaysNamesSomebody` — five cases including a contact route missing its kind and one missing its value. The handler additionally requires the named party to resolve to a Party, and that check is **untested** | Unit | partial |
| Composition choices are recorded with a decider, and their vocabulary is L2 | [#172](../../issues/172) | `TestRecordNamesAreSlugsAndNothingMore` — nine cases. The core has no vocabulary to check a choice name against (that is the layering test's answer here), so it checks only that the name is a name; the choice and its value are stored opaquely and there is no enum of postures or origins anywhere in CREST | Unit | covered |
| §16's "a worker declining work is recorded nowhere" survives F2 | [#172](../../issues/172) | `TestDecliningWorkIsRecordedNowhere` now takes a JSON-pointer exemption rather than a whole-file skip, and `withoutSubtree` fails loudly if the exempted path is renamed. A future field on `Context` that did record a worker declining work still fails the guard — the point of narrowing the carve-out rather than widening the skip | Contract | covered |
| An invitation is an offer that ends, of something real | [#182](https://github.com/theflywheel/CREST/pull/182) | `TestAnOfferMustBeAnOfferOfSomethingThatEnds` — seven cases: the reference offer sends, no invitee, no functions, a blank function, no period, a period with no end (the grant an acceptance creates must end, so the offer must), and a document-sized note. A new offer is SENT and a start defaults to when it was sent. **Handler paths (invitee must be an organisation Party, configurator write gate) are untested**; they reuse the gates #172 tested as predicates | Unit | partial |
| Declining an invitation records who and why and destroys nothing | [#182](https://github.com/theflywheel/CREST/pull/182) | `TestDecliningRecordsWhoAndWhyAndDestroysNothing` (actor, reason, offer substance intact), `TestARefusalWithoutAReasonIsRefused` (empty and document-sized reasons), `TestASettledAnswerStaysSettled` (second answers in every order refused with `errInvitationDecided`). The trail (`invitation_events`) is append-only in SQL; **the sequence under concurrency is unproven**, same standing as the ownership trail | Unit | partial |
| Registration works before OR after an invitation (g2_5) | [#182](https://github.com/theflywheel/CREST/pull/182) | `TestRegistrationWorksBeforeOrAfterAnInvitation` — six orderings over `mayAcceptInvitation`, the same pure function the handler calls: no registration, APPLIED, TERMS_ACCEPTED and REJECTED all wait (`errAcceptanceNeedsApproval`, said as "not yet", never "no"); APPROVED accepts in both orders; and sending never waits on registration — `newInvitation` takes no registration argument by design. **The acceptance-creates-the-grant transaction (grant + enqueue + invitation in one tx) is handler code with no test**; the narrowing itself is `narrowerThanTerms`, already covered | Unit | partial |
| A question belongs to the open offer | [#182](https://github.com/theflywheel/CREST/pull/182) | `TestQuestionsBelongToTheOpenOffer` — a question on an open offer lands on the trail with its asker; an empty question and a question after the answer are refused. The either-side gate (invitee, owner or named configurator) is handler code, **untested** | Unit | partial |
| A terms request names a published version and declares, never uploads | [#182](https://github.com/theflywheel/CREST/pull/182) | `TestARequestNamesAPublishedVersionExactly` (no id, version 0) and `TestADeclaredDocumentIsAReferenceNeverTheDocument` — five refusals including a multi-line ref (content wearing a reference's name) and a ref past the 500-byte bound. There is no blob store and no upload path anywhere in parties, per finding [#180](../../issues/180); where declared refs point is a stated open question | Unit | covered |
| The wider-terms request machine: draft, submit, withdraw, decide | [#182](https://github.com/theflywheel/CREST/pull/182) | `TestOnlyADraftIsEditable` (edit after submit refused, double submit refused), `TestWithdrawIsTheOrganisationsOwnActWhileItWaits` (withdraw a draft refused, withdraw after decision refused, actor recorded), `TestTheDecisionHasANameOnItAndItIsNeverTheApplicants` (approve, deny with reason, deny without one refused, self-decision refused, deciding a draft refused, re-deciding refused). **The approval's registration move (`applyApprovedTerms`, previous terms captured on the request) is SQL + handler, unproven by test** | Unit | partial |
| A check is a recorded verdict with an owner, never fake automation | [#180](../../issues/180) | `TestACheckVerdictHasABinaryOutcomeAndAnOwner` — a policy-owned PASS and a party-owned FAIL record; refused: a third outcome, an unknown owner kind, an unowned verdict, the applicant owning or recording verdicts on its own request, a nameless check, a check on a draft and a check after the decision. No automated checker exists and none is simulated; the business-register adapter is a future policy-owned writer (blueprint §15 J1, corrected per the finding) | Unit | covered |
| No project record carries a personal identifier | [#172](../../issues/172) | `TestNoProjectFixtureCarriesAPersonalIdentifier`, and the fixtures themselves: every identifier in `projects_test.go` is a synthetic DID spelled in the ULID alphabet, and the coverage string is a place name. Nothing in the project surface has a column, a field or a payload path a national identifier or biometric could reach | Unit | covered |

## Evidence and confirmation

| Feature | Issue | How it is proven | Layer | Status |
|---|---|---|---|---|
| Source heartbeat: a feed that goes quiet is noticed | [#22](../../issues/22) | `TestASourceThatGoesQuietIsNoticedAndItsOwnerIsTold` (E2E): the feed sends, the clock moves past its declared cadence, the sweep discovers it, and **the owner is actually told** — the message is asserted at the SMS mock, not at the 201. This is the only failure in the system a worker cannot see or report, so it is the only one that has to be noticed unaided | E2E | covered |
| An outage alerts once per episode, not once per sweep | [#22](../../issues/22) | The same scenario sweeps twice with nothing fixed and asserts the second reports `stillQuiet`, not `wentQuiet`. One message per sweep is how an alert channel becomes something people mute, and the muted channel is the one the real outage arrives on. `TestAFeedThatResumesIsHealthyAgain` asserts the episode closes, so a later outage is discovered again rather than suppressed | E2E | covered |
| Source state is derived, never stored | [#22](../../issues/22) | `TestSourceStateIsDerivedFromTheClock` reads the identical row as HEALTHY then SILENT with nothing but the clock moving; `TestSilenceBeginsAtTheDeclaredCadence` pins the boundary to what the deployment declared rather than a multiple invented in code | Unit | covered |
| A source cannot be monitored without a cadence and an owner | [#22](../../issues/22) | `TestASourceCannotBeMonitoredWithoutACadenceAndAnOwner`. A cadence inferred from history learns from a degraded feed and calls it healthy; an owner defaulted to nobody produces an alert that gets forwarded until it is nobody's | E2E | covered |
| CSV adapter golden files | [#41](../../issues/41) | Golden CSVs: clean, malformed (missing mandatory column), mandatory-core-only, source-asserting-provenance, duplicate, unmatched-identity — all six under `tests/fixtures/`, read by `tests/contract/adapter_test.go` and `adapters/csv/csv_test.go` | Contract | covered |
| Registry reuse (g4_7) tells "no reuse yet" apart from "no data yet" | — | `GET /v1/registry-reuse` joins `claims` to `units` on `unit_id` and counts, per party, how many distinct `unit.contextId` values claimed against them (any claim state — a dispute contests the record, W5, it does not erase that the submission happened); `reusedParties` is those spanning more than one context, `reuseRate = reusedParties / totalClaimedParties`, and an empty registry reports `reuseRate: null` rather than a fabricated 0. `TestReuseMetricOnAnEmptyRegistryReportsNilRateNotZero`, `TestReuseMetricCountsOnlyPartiesSpanningMoreThanOneContext`, `TestReuseMetricIsZeroWhenNobodyIsClaimedByMoreThanOneContext` cover the computation as a pure function | Unit | spike |
| Evidence receipt (w6_3) composes existing ingestion records, stores nothing new | — | `GET /v1/batches/{id}/receipt` reads the existing batch, its units and their claims, and every unclear row the batch produced (resolved or not), and marks queue position from the same ordering `GET /v1/unclear` already uses. w6_1/w6_2 (the scoped request and file-back upload) stay `illustrative` in `docs/journey-traceability.json` — no scoped-link or upload endpoint exists to receipt against, so this answers "what did the one real ingestion door (`POST /v1/batches`) bring in" rather than inventing a per-contact record. `TestBatchReceiptMarksQueuePositionOnlyForOpenRows`, `TestBatchReceiptLeavesAResolvedRowWithNoQueuePosition`, `TestBatchReceiptComposesCleanlyWithNothingUnclear` | Unit | spike |
| A repeated row is passed through, not judged by the adapter | [#41](../../issues/41) | `TestDuplicateRowsArePassedThroughIdenticallyRatherThanJudged` against `csv-with-the-same-record-twice.csv` — three rows in, three records out, the repeat identical in every field the dedupe key reads and the distinct row not | Contract | covered |
| Deduplication is on content, and only on content | [#41](../../issues/41) | `TestTheSameRecordTwiceCollidesOnTheDedupeKey` — the same work down a second transport collides; six distinguishing mutations (outcome, record ref, worker, period, context, definition version) each must not | Unit | covered |
| A national identifier is hashed before anything is stored | [#41](../../issues/41) | `TestRedactReplacesANationalIdentifierBeforeAnythingIsStored` — redaction yields the salted hash the registry matches on, and leaves a phone number alone | Unit | covered |
| Validation against ACTIVE definition | [#22](../../issues/22) | Record violating the definition rejected with a usable reason | Unit | planned |
| Identity matching via hashed keys | [#22](../../issues/22) | Match provenance records *which key matched*; ambiguous match → unclear queue | Unit | planned |
| Unclear queue has an owner | [#22](../../issues/22), [#25](../../issues/25) | Queue depth is observable via `GET /v1/unclear`, and a row is now workable: `POST /v1/unclear/{id}/resolve` closes it against a named custodian | E2E | covered |
| T=7 state machine | [#23](../../issues/23) | All transitions incl. disallowed ones; clock driven by harness, never slept | Unit | planned |
| A worker who cannot read consents in their own voice | [#24](../../issues/24) | `harness/scenarios/registry_test.go` — a recording is stored against the enrolment, played back byte-identical, and the capture names who took it | E2E | covered |
| A voice consent with no recording is refused | [#24](../../issues/24) | 400 naming what is missing, and a database constraint saying the same thing. For a non-literate worker this would be their entire consent record | E2E | covered |
| Withdrawal deletes the recording and keeps the record | [#24](../../issues/24) | The row survives and reads WITHDRAWN; the artefact 404s. The row makes it auditable, the deletion makes it real | E2E | covered |
| Withdrawing consent stops new evidence and keeps the old | [#24](../../issues/24) | `harness/scenarios/spine_test.go` — a batch after withdrawal is refused with consent named as the reason, and the claim recorded before it is still there. §9: consent governs what may be collected next, never what was | E2E | covered |
| One programme's consent is not another's | [#24](../../issues/24) | `TestConsentGivenToOneProgrammeDoesNotCoverAnother` — consent to one context reads GRANTED there and NONE in a second; both can be held side by side. Corrects the deployment-wide scoping first shipped | E2E | covered |
| A claim already in its T=7 window is unaffected by a withdrawal | [#24](../../issues/24) | The consent gate sits at ingest, so a window opened under live consent still exits and still releases payment. Decided rather than inherited: the work was done and recorded while consent stood | E2E | covered |
| A worker with no phone completes enrolment | [#24](../../issues/24) | `TestAWorkerWithNoPhoneCanStillBeEnrolled` — shipped with #20; being vouched for raises no assurance | E2E | covered |
| Worker DID at enrolment | [#24](../../issues/24) | Every party is minted `did:crest:party:<ULID>` including assisted enrolments, asserted throughout the harness. **Blueprint §4 still says `did:crest:worker:`, which #55 corrects** | E2E | covered |
| A worker without a phone gets a card carrying the whole record | [#24](../../issues/24) | `harness/scenarios/spine_test.go` — Worker C joins on a roster id, confirms, and `GET /v1/credentials/{id}/card` returns a payload that decodes back to a credential which still verifies and still derives a tier. The card carries no tier and no identifier | E2E | covered |
| The printable card needs no network to print | [#24](../../issues/24) | The QR is inlined as a data URI and the page references nothing over HTTP — a card whose image is fetched from a server prints as a broken icon exactly where there is no internet | E2E | covered |
| MOSIP's own reader reads the card CREST prints | [#24](../../issues/24) | `make printed-card` feeds a Go-produced payload to `@mosip/pixelpass` and requires the credential back unchanged. The interop direction a Go test cannot check, and the only one that matters for a card read by somebody else's scanner | Spike | spike |
| We read the format MOSIP's library emits | [#24](../../issues/24) | `pkg/credential/pixelpass_test.go` decodes a card their library produced. **A card holding JSON comes back as CBOR, not JSON** — asserted as the current behaviour and tracked in [#92](../../issues/92) | Unit | partial |
| A credential survives a round trip through a card | [#24](../../issues/24) | A real Certify-issued credential encodes, decodes and still verifies. Compression that round-trips but perturbs a byte would pass a naive check and fail in front of a verifier | Unit | covered |
| A month of real-shaped evidence, end to end | [#25](../../issues/25) | `make poc` against a live stack: 47 rows in, 40 accepted, 7 unclear, 1 deduped, 39 credentials, 39 cards, 38 payments released and 1 held with a reason. Asserts every exit released payment, nothing was silently dropped, and every held payment has an owner | Spike | spike |
| A source system's own column names, by configuration only | [#25](../../issues/25) | `tests/contract/adapter_test.go` — the same work in two vocabularies becomes the same canonical record, including the enrichment fields a tier map reads, with identical code and no L1 change | Contract | covered |
| A mapping cannot argue for its own tier | [#25](../../issues/25) | Nothing in a mapping names `sourceClass`, `captureMethod` or `adapterRef`; the two records' provenance is asserted identical | Contract | covered |
| A DHIS2-shaped export runs end to end | [#25](../../issues/25) | `make poc-dhis2` — an export using `event`, `eventDate`, `orgUnitName` and named data elements produces the same 39 credentials, 39 cards and 38 released payments as the canonical batch | Spike | spike |
| G2 · a real CSV from a real source system | [#25](../../issues/25) | **Not proven.** `make poc`'s batch is authored in this repo, and #25 asks for no hand-authored data anywhere in the path. Needs a partner export | E2E | planned |
| The physical scan | [#66](../../issues/66) | **Not proven.** Printed on paper and scanned on a real device with its radios off. Needs a person, a printer and a phone | E2E | planned |

## Payments and verification

| Feature | Issue | How it is proven | Layer | Status |
|---|---|---|---|---|
| Rate-owner assignment (f1_2) | — | `TestNobodyAssignedMeansNobodyAuthors`, `TestOnlyTheAssignedOwnerAuthors`, `TestAnAssignmentRecordsBothNames` (unit, `services/payments/funders_test.go`): authoring with no assignment or by a non-owner is refused; an assignment records assigner, assignee and date; re-assignment supersedes and keeps history (partial-unique index `rate_owner_current`). Driven through the console in the Playwright walk `tests/e2e-apps/apps.spec.js` "the funders walk — rate as terms, held with an owner, released by the last gate": the Org Admin assigns on `#/rateowner` (f1_2), the assignment lists with assigner and date, and a non-owner's publish is refused 403 `not_rate_owner` | Unit + Playwright (`make e2e-apps`) | spike |
| A rate is versioned terms, read at the release moment (f1_3, f1_4) | — | `TestANewRateIsANewVersionNeverARewrite`, `TestPaymentsReadTheVersionInForceAtTheReleaseMoment`, `TestARatePublishedForTheFutureIsNotYetInForce` (unit); `TestPaymentSetupCarriesAuthoringProvenance`, `TestPaymentSetupWithoutProvenanceStillValidates`, `TestPaymentSetupRefusesUnknownFields` (contract): each publication is a new `payment-setup` LinkedRecord version with author and supersession, no update route exists, and `amountFor` prices by the version in force at `releasedAt` — a later publication never reprices done work. The walk publishes through `#/rate` → `#/ratepublish` (f1_3, f1_4), asserts the new version names what it supersedes, that NO edit affordance exists on a published version, and that the released instruction is priced by the version in force at its own release moment (3 × 175.00 = 525.00 KES asserted to the minor unit). `no_rate_in_force` remains unit-only | Unit + Contract + Playwright (`make e2e-apps`) | spike |
| Half done is a real state (f1_5) | — | `TestHalfDoneIsARealState` (unit): standing is derived, never stored — `configured-not-live` between mechanism creation and activation; `GET /v1/mechanisms/by-context/{id}` answers `not-configured` as a 200, not an error. `#/ratestanding` (f1_5) renders all three standings as real screen states and the walk asserts `not-configured` before any mechanism exists and `configured, not live` after creation | Unit + Playwright (`make e2e-apps`) | spike |
| One test disbursement, recorded with its result (f2_4) | — | `TestAFailedTestSatisfiesNothing` (unit): only a SUCCEEDED test satisfies the proving condition; a FAILED one is recorded with a reason and owner (DB constraint `failed_tests_have_a_reason_and_an_owner`). The walk sends one real test payment through the mock rail from `#/mech/test` (f2_4) and asserts the SUCCEEDED record with its rail reference; a test naming no destination is refused loudly on-screen. A FAILED rail result's reason+owner rendering exists on the same screen (unit-proven; not forced E2E because the sandbox rail does not fail on demand) | Unit + Playwright (`make e2e-apps`) | spike |
| The reconciliation file contract (f2_5) | — | Format `crest-recon-csv-v1`: CSV, one line per instruction, tied back by instruction id, claim id and rail ref; columns append-only. Declared in `reconciliationFileColumns` and the OpenAPI contract; agreement is a recorded act that gates activation. `#/mech/recon` (f2_5) fetches the real export and the walk asserts the `crest-recon-csv-v1` format and the `instruction_id`/`claim_id` tie-back columns on the live file, then records the agreement | Playwright (`make e2e-apps`) | spike |
| An advisory statement states its limits (f2_6) | — | `TestAStatementStatesItsLimits` (unit): every statement carries a non-empty `limits` list saying it is advisory and the rail's statement is authoritative; held payments appear with reason and owner rather than going missing. `#/mech/statement` (f2_6) renders the live statement with each limit on its face; the walk asserts the advisory limit and the held-payments limit verbatim | Unit + Playwright (`make e2e-apps`) | spike |
| The batching choice records who chose and the trade-off (f2_7) | — | `TestABatchingChoiceRecordsItsTradeoff` (unit): a choice with the window or the worker's cost unstated is refused; the window value itself is L2 and unread. On `#/mech/batching` (f2_7) the walk records a choice with the trade-off unstated (refused loudly), then with it, and reads back who chose, when, and the sentence. The reference frame's "Hold payment if a dispute is open" field is answered honestly on the screen — never, W4 — instead of pretending the choice exists | Unit + Playwright (`make e2e-apps`) | spike |
| Mechanism activation gate (f2_8, f2_10) | — | `TestEveryUnmetConditionRefusesActivation` (each of the four conditions individually refuses, with the conditions readable), `TestActivationIsAnActWithAnActor`, `TestAllConditionsMetActivates`, `TestActivatingALiveMechanismIsIdempotent`, `TestFactsReadTheRecordedActs` (facts come from recorded acts, never assertions; verifying an unsubmitted qualification is a 409). The walk activates from `#/mech/activate` (f2_8): refused readably with all four conditions listed before the acts, every condition shows its satisfying record's date after them, and activation flips ACTIVE and releases the held instruction — `#/mech/live` (f2_10) shows what the gate opened, and the service read confirms the re-priced RELEASED amount | Unit + Playwright (`make e2e-apps`) | spike |
| The gate sits in front of disbursement, not the window exit (f2_9) | — | `TestNoMechanismMeansNoGate`, `TestALiveMechanismDisburses`, `TestANotLiveMechanismHoldsWithAReasonAndAnOwner` (unit): a window exit ALWAYS creates the instruction — `holdForMechanism` can only turn it HELD with code `mechanism_not_live`, a sentence, and the mechanism owner named (W10 asserted field by field). The release handler applies it after pricing, never before creating. **Now driven end to end**: the walk confirms real work (batch → window → the worker's own confirm exit) while the mechanism is CONFIGURED and not live, asserts the exit still created its instruction — HELD `mechanism_not_live` with the owner named — sees it on the invariant screen `#/mech/qualify` (f2_9) with the boundary stated (all four exits release; only disbursement waits), then watches activation release that same instruction | Unit + Playwright (`make e2e-apps`) | spike |
| The funders console screens render for their personas | — | `frontend/apps/console/src/views/Funders.tsx`: eleven screens (f1_2–f1_5 on `#/rateowner`, `#/rate`, `#/ratepublish`, `#/ratestanding`; f2_4–f2_10 on `#/mech/test`, `#/mech/recon`, `#/mech/statement`, `#/mech/batching`, `#/mech/activate`, `#/mech/qualify`, `#/mech/live`), on the reference's five-entry setup rail like the J3 actors. The rate-owner and payment-mechanism-owner persona walks in `tests/e2e-apps/apps.spec.js` visit every route signed in (identity asserted in the appbar, no redirect, no error banner) including the honest empty states before any mechanism exists | Playwright (`make e2e-apps`) | spike |
| The F-2 walk's first three screens (f2_1–f2_3) | — | `MechWhere` (`#/mech/where`) answers where CREST stops by writing the project's `payment-posture` composition record — the same record the Configurator's composition screen writes, asserted in the funders walk (picking "At a payment instruction" records and routes to rails); `MechRails` (`#/mech/rails`) records `rails-chosen` on the mechanism (creating it if absent) and `MechConnect` (`#/mech/connect`) records `provider-connected`, both new whitelisted mechanism record kinds in `services/payments` (`go test ./services/payments/...` green); the walk asserts each recorded act renders with its actor and date | Playwright (`make e2e-apps`) + unit | spike |
| The field enrolment screens carry the w2 pack's copy (J6) | — | `frontend/apps/field/src/screens/Enrol.tsx`: w2_1's done-today/to-sync counters derive from the device's own queue state and the offline sidecar renders only when offline; w2_3's consent script carries ownership and per-use consent with the recording sentence naming agent ID and time; w2_4's hold states the custodian-only authority verbatim; w2_5's no-phone card sidecar renders only for a phoneless registration. Route sweep + copy pins in `tests/e2e-apps/apps.spec.js` "enrolment app: every route" | Playwright (`make e2e-apps`) | spike |
| The worker screens carry the w1 pack's copy (J7) | — | `frontend/apps/worker/src`: the entry fork's same-DID promise and the Section 11 third-route note; the consent screen's three-line agreement (w1_5); the confirmation card's auto-confirm date and burden-of-proof sidecar (w1_10); the dispute's officer line, mismatch options and 14-day deadline (w1_11); the pay buckets and held-payment lede (w1_23/24); the show-face's disclosure limits (w1_18); the pre-anchor w1_16 face on /added with real counts. Pinned in `tests/e2e-apps/apps.spec.js` "worker screens carry the w1 pack's copy (J7)" | Playwright (`make e2e-apps`) | spike |
| The attestation surfaces carry the J8 pack's copy (w3/w6) | — | `frontend/apps/field/src/screens/Attest.tsx`: the worklist names the auto-resolve exit; the assisted-confirm sidecar states CREST has no number-entry screen; the handoff draws w3_5's four-step chain ending "no person signs it" with the per-project validator note. `frontend/apps/verify/src/screens/Panel.tsx`: w6_1's scoped-template promises, w6_2's three-answer row with provenance sentence, w6_3's unresolved fork drawn beside the §16 ruling. Pinned in "enrolment app: every route" and "verify app" sweeps | Playwright (`make e2e-apps`) | spike |
| Funder role derived from ownership, not a seeded label | F-1/F-2 wave | The payments service answers `GET /v1/rate-ownerships/mine` and `GET /v1/mechanisms/mine` from the caller's own resolved party (never a party named in a query), so the console's eSignet login derives the rate-owner / payment-mechanism-owner persona from the real assignment and ownership records rather than a hardcoded persona id. Asserted in the funders walk: the assigned owner's own read names the definition (a non-owner's does not), and the mechanism owner's own read names their mechanism | Playwright (`make e2e-apps`) | spike |
| The oversight dashboards carry the reference's anatomy over live reads (J9 p2_11–16, J10 v4_1–2, J11 g4_4–7) | Oversight pack | `frontend/apps/console/src/views/Dashboard.tsx`, `Admin.tsx` (`Portfolio`, with the v4_2 drill opening on the same route), `Custodian.tsx`: the pack's stat sub-labels, the "Worst gaps first" work list sorted by gap-to-close, the duplicates queue's four figures (open pairs, rate against registered, median time to resolve from closed holds, merges-without-confirmation), the reuse one-vs-two-or-more split, the report's stage rows and Cuts card, and every explanatory callout — each over the existing reads (`/v1/claims`, `/v1/instructions`, `/v1/holds`, `/v1/coverage`, `/v1/quality-worklist`, `/v1/registry-reuse`). Fixture figures the reference draws (Turkana, KES, 84.4%) are never rendered; a figure with no contract reads "—" and says which. Controls the design draws but nothing backs (Export the bundle, Bulk review, Raise an assurance query, Preview, Save and share) state so on click. Invariant (payments): the "Open the N" list on p2_14 is exactly the held instructions with no owner — a non-empty list is a defect surfaced, not a feature | Playwright (`make e2e-apps`) — one walk across the three personas pins the copy and drives every not-backed control; the persona walks cold-load all ten routes | spike |
| The first-login bootstrap gets a front door: party invitations (#123) | [#123](../../issues/123) | `services/core/parties/partyinvites.go` + migration 0016: `POST /v1/parties/{id}/invitations` (the instance operator or an APPROVED organisation, for an unbound party) mints a one-time code — only its hash is stored, with the inviter's name, an expiry defaulted to 7 days and capped at 30 — and `POST /v1/party-invitations/claim` appends the claimant's OWN subject to the invited party through `appendBinding`, refusing an unknown, expired or spent code and a party bound meanwhile, each by its own name. `POST /v1/organisations` returns the applicant's claim code for the organisation it just registered (g2_13's "copy the key"). `tools/bootstrap-operator` writes the one party no door can create — the operator — and mints its claim. Invariant (verification): no widening of #102 — the party must be as unbound as the bare bootstrap already accepts, and the claimant binds nobody's subject but their own. `TestInviteCodeIsLongLowercaseBase32`, `TestInviteCodeHashIsCaseAndSpaceInsensitive`, `TestInviteTTLIsDefaultedAndCapped`, `TestInviteAdmissibleRefusesEachWayForItsOwnReason`, `TestASpentInvitationSaysSpentNotBound` cover the decisions as pure functions; `TestAnInvitedPersonClaimsTheirRecordWithTheirOwnLogin` and `TestARegisteringOrganisationCanClaimItselfWithItsOwnLogin` (harness/scenarios) drive the real routes: create → invite → a person cannot invite → wrong code 404 → claim (code typed in any case) → `/v1/auth/me` resolves → spent code 409 → re-invite of a bound party 409; and the open-door registration's code claimed by the applicant. Console: `#/claim/:code` → eSignet → the return leg claims and re-derives the persona from the grant (`views/Claim.tsx`, `SignIn.tsx`); People & roles `Invite a person` (party → invitation → grant, link shown once); the onboarding status screen's claim block; `ensureOrgSession` no longer mints a dev token for a claimed organisation outside the local stack | Unit + E2E (`make test-e2e`) + Playwright (`make e2e-apps`: an org admin invites an author, a stranger claims by the return leg and lands as Work Definition Author, a second stranger is told the code is spent) | spike |
| The enrolment and worker doors run on a clean deployment: eSignet in, no fixture party or project | [#123](../../issues/123) | `frontend/apps/field`: the login is `Continue with eSignet` (`#login-esignet`) with the dev shortcut (`[data-login]`) rendered only on the local stack; `#/auth` completes the return leg through `/v1/auth/me`, an unbound identity is told it holds no role here (an agent is invited by their organisation through the console — no self-registration on this door), and the session is restored from `crest.field.session`. Every `FIX.*` is gone: `enrolledBy`, `capturedBy`, `assistedByPartyId`, `submittedBy` and the no-phone contact route are the signed-in party; the project is chosen from the distinct contexts of `GET /v1/authorizations/mine` (`#context-select`, auto-picked when there is one, an honest `#no-project` when none), the roster's definition from `GET /v1/definitions?state=ACTIVE` (`#definition-select`) — never a hardcoded id. `frontend/apps/worker`: a stranger cannot list programmes (the registry refuses an unnarrowed project listing), so self-registration takes the programme reference the worker was given (`#programme`, prefilled by `#/join/:contextId`) and records enrolment consent against it, or creates the record without consent and says a programme will record it when it enrols them; the wallet derives its definition from the worker's own windows; vouching reads the confirmer's own authority from `/v1/authorizations/mine`. Invariant (confirmation): the assisted exit still names the assisting party; the dispute path is untouched | Playwright (`make e2e-apps`): the enrolment walks now pick the story project through `#context-select`; the worker walks unchanged. Not yet driven: a real eSignet arrival on the field door (needs the invitation flow end to end — the clean-slate walk on the deployed fleet) | partial |
| The operator publishes terms from the console; nobody else can | [#123](../../issues/123) | `POST /v1/terms` was open to any caller and had no console surface, so a clean deployment could admit nobody — an admission is an acceptance of a named terms version. It now requires the caller to be the instance operator named in the published self-description (`not_the_operator` otherwise), assigns the id and first version, and the operator's `#/instance/consent` screen carries "Terms an organisation can be on": the published sets, and a form (name, permissions) that publishes a new one (`frontend/apps/console/src/views/G1.tsx`). Invariant (verification): terms cap what an organisation's people can be granted, so who may publish them is who may widen every grant's ceiling — the operator, never the applicant | Playwright (`make e2e-apps`): the instance persona publishes a set and sees it listed; a custodian's `POST /v1/terms` is refused 403 `not_the_operator` | spike |
| The organisation stands up F-1/F-2 before its owners exist; personas read from records | [#123](../../issues/123) | On a clean deployment nobody derives as rate owner or mechanism owner until the organisation has assigned one and created the other, and the funders routes were gated to those personas. The org admin now holds the funders routes (`App.tsx` `allowed()`), Payment set up carries two stand-up buttons (`[data-act="to-rateowner"]`, `[data-act="to-mech"]`), and the rails screen names the mechanism's owner (`[name="mechowner"]`, yourself by default) so `POST /v1/mechanisms` is made with `createdByPartyId` = the org and `ownerPartyId` = the person who will run it — the same authority check the service already makes. Two more personas are derived from real records rather than defaulting to Org Admin: a `resolve-unclear-evidence` grant → Registry Custodian; being named as a project's configurator (`GET /v1/projects?configuratorPartyId=`) → Project Configurator; the invite form offers the custodian's function. Invariant (payments): every held payment names an owner — the owner named here is who it will name | Playwright (`make e2e-apps`): the org-admin persona walk still holds every J3 route; the funders walk still creates the mechanism as its owner by default. Driven for real on the deployed fleet by the clean-slate demo scripts (`tmp/prod/04-j3-org-admin.mjs`, `05-claims.mjs`, `07-funders.mjs`): Alice derives Project Configurator after being named on the project, Amina and Ndegwa derive their P-3 roles from their grants. What is not yet asserted by a committed test: the orgadmin-owned mechanism creation path and the two new derivations — recorded here as a gap | partial |
| PaymentInstruction idempotency | [#26](../../issues/26) | Duplicate emission pays once — asserted at the rail sandbox, not just in-process | Unit + E2E | planned |
| Reconciliation with owned reasons | [#26](../../issues/26) | Injected gaps of each class; every one lands with an owner | E2E | planned |
| Advisory mode (no rail) | [#26](../../issues/26) | Full flow with rail disabled produces a statement, not an error | E2E | planned |
| A credential carries its own resolvable pin to its definition | [#16](../../issues/16) | `TestACredentialCarriesItsOwnPinToTheDefinition` (E2E) asserts the signed `definitionProof` names the registry version and digest the definition was actually published as, and that the verifier's trust chain follows it. A pin to the *wrong* version is worse than none — it resolves, it verifies, and it describes a definition the credential was not measured under, so the version and digest are both checked against the publication | E2E | covered |
| Absence of a pin is an answer, not a gap | [#16](../../issues/16) | The same scenario asserts that a deployment with no transparency log emits **no** `definitionProof` rather than an empty one. An empty object reads as a pin nobody can follow; saying nothing says the verifier must trust the issuer | E2E | covered |
| A signature never carries a node address | [#16](../../issues/16), [#70](../../issues/70) | Asserted on the signed pin. Where the log lives is the deployment's published self-description, which can be corrected; an address inside a signature is a redirect nobody can ever withdraw | E2E | covered |
| A credential names the chain a verifier walks | [#16](../../issues/16) | `TestACredentialNamesTheChainAVerifierWalks` (E2E): `claimId`, `skillCode` and `issuerAuthority`, and **both authorization references resolve on the registry** — because a chain ending at something a verifier cannot resolve is worse than one ending nowhere, since it looks like it leads somewhere | E2E | covered |
| The issuer authority names both scopes of one primitive | [#16](../../issues/16) | The same scenario asserts the instance-scoped and context-scoped references differ. §2 collapsed Qualification and ProjectGrant into one Authorization at two scopes; §8's sketch still named them as two primitives, and reading it literally would have built the model §2 removed | E2E | covered |
| A credential distinguishes the claim from the unit | [#16](../../issues/16) | Asserted directly: `claimId != eventId`. A Unit says the work happened, a Claim says who performed it, and a disputed claim must never destroy the Unit — which only holds if the two are separately addressable from the credential | E2E | covered |
| A dispute never revokes the credential | [#58](../../issues/58) | `TestADisputeDoesNotRevokeTheCredential` (E2E): a claim confirmed, a credential issued, then the claim disputed — the credential stays `valid`, the status list bit is untouched, and the verdict reports the dispute. Before this the credential stood with the dispute invisible, which #58 called the one combination nobody would pick deliberately | E2E | covered |
| A verifier learns the standing, not the reason | [#58](../../issues/58) | `TestAVerifierLearnsTheStandingNotTheReason` asserts on the **serialised** verdict and on the endpoint body, because a field with no Go name is still in the JSON. A dispute reason is ordinarily the worker speaking about their own record | E2E | covered |
| Contests cannot be listed unscoped | [#58](../../issues/58) | `TestContestsCannotBeListedUnscoped`. An unscoped listing is a search for disputed workers, and `targetKind=unit` is refused because a contest against a Unit is not expressible at all | E2E | covered |
| Trust-chain walk | [#27](../../issues/27) | Broken chain at each link rejected: credential → definition → authorization → org → instance | Unit | planned |
| Every trust-chain link says whether a verifier can check it | [#68](../../issues/68) | `TestALinkAlwaysSaysWhereOrWhat` and `TestIssuerLinkInheritsTheDefinitionsCheckability` (unit); the spine asserts every link either names where to check it or names what is being trusted instead. A chain given as a list of sentences conflates facts a verifier can confirm with facts CREST is telling them | Unit + E2E | covered |
| The definition link is checkable exactly when the definition reached a log | [#68](../../issues/68), [#21](../../issues/21) | `TestTheTrustChainSaysWhichLinksAVerifierCanCheck` compares the link's `checkable` against the publication's `transparent`, and — when checkable — **fetches the URL it points at**. A `how` nobody can fetch is the promise this field replaces. Passes in both modes: node and fallback | E2E | covered |
| A valid verdict states what it does not establish | [#68](../../issues/68) | The same scenario asserts `notEstablished` names the subject's authorization. A green verdict reads as "and this person was authorised to do this work", which is exactly what a deployment cannot demonstrate to a stranger — a disclosed limit is not the same as one the verifier discovers by assuming wrongly | E2E | covered |
| Per-request disclosure consent | [#27](../../issues/27) | Built as the presentation-request flow (w1_15, w1_19, w1_20): `POST /v1/presentation-requests` refuses an unproven requester or a purpose outside 10–200 chars, the worker decides per share against the resolved disclosure list, the verifier collects exactly the approved ids once. State machine unit-tested (`sharerequests_test.go`: `TestARequestNamesWhoIsAskingAndWhy`, `TestTheWorkerApprovesOnlyFromTheListTheyWereShown`, `TestASettledAnswerStaysSettled`, `TestAnExpiredRequestIsNotAnswerableOrCollectable`, `TestConsentIsPerShareEveryTime` — refusals in every table). Proven end to end by the #190 presentation walk: collect-before-decision 409, approve-a-subset, collect exactly that subset once (second collect 409), decline-with-reason leaves nothing collectable — the walk that surfaced and now guards the `presentations.scope` CHECK gap | Unit + `make e2e-apps` ("per-share consent: the presentation loop on both faces, and the decline path") | covered |
| The worker sees the same list the verifier does | [#27](../../issues/27) | Structural, then asserted: one function (`disclosureList`) resolves the list for both faces of `GET /v1/presentation-requests/{id}`, an approval may only name ids from that list (`TestTheWorkerApprovesOnlyFromTheListTheyWereShown`), and `collect` re-reads the approved list from the record and serves nothing else. An unresolvable list is said plainly (`disclosureListError`), never served empty. The #190 walk reads both faces against the real services: the worker's sent view and the verifier's collect agree on the approved subset and its count | Unit + `make e2e-apps` ("per-share consent: the presentation loop on both faces, and the decline path") | covered |
| A consented share reaches the worker's check trail | [#27](../../issues/27) | Each collected credential writes its own `presentations` row (`scope=consented`, `outcome=shared`) with the requester and purpose. The #190 walk asserts the trail: after the collect, the worker's `#/profile/checks` shows the share | `make e2e-apps` ("per-share consent: the presentation loop on both faces, and the decline path") | covered |
| Batch verification caps | [#27](../../issues/27), [G1 #9](../../issues/9), [#107](../../issues/107) | #107 built the batch half — size cap, declared purpose, per-worker trail entries (rows above). What G1 #9 asks beyond that stays open: a **rate** cap across the single-check endpoint, so the loop is bounded like the batch, and a distinct authorization scope for bulk checking. Without those, the cap moves the traffic rather than bounding it | Unit + E2E | partial |
| A verifier resolving a party sees the chain's credentials, not the merge | [#104](../../issues/104) | `TestAVerifierSeesTheWholeChainsCredentialsAndNotTheMerge` (E2E): both ids resolve to both credentials after a merge; the raw response bytes carry no merge vocabulary and no identifier list; the registry's id-mapping refuses an anonymous caller and answers the worker. The residue — credentials name their subjects — is stated in §16, not papered over | E2E | covered |
| Provider secrets are never storable in a primitive | [#105](../../issues/105) | `TestNoPrimitiveHoldsProviderSecretMaterial` (contract) refuses any schema property named like secret material unless it is a reference or fingerprint; the guard-secrets hook covers the repo the same way | Contract | covered |
| A batch check needs a purpose and stays under the cap | [#107](../../issues/107) | `TestABatchCheckNeedsAPurposeAndStaysUnderTheCap` (E2E): no purpose 400, anonymous 400, over-cap 413 naming the number, a bounded batch answers per credential, and the worker's check trail gains one entry **per credential** — the assertion that would otherwise pass by accident. `TestTheBatchCapCanBeMovedButNotRemoved` (unit) pins that a non-positive cap is refused | Unit + E2E | covered |

## Operations

| Feature | Issue | How it is proven | Layer | Status |
|---|---|---|---|---|
| Harness runs from clean checkout | — | CI runs `make test-e2e` on a fresh runner; no manual step | E2E | planned |
| Deployment verification | — | `make verify-deploy ENV=staging` green after every deploy | Deploy | planned |
| Second adapter via config only | [#30](../../issues/30) | Adapter added with **zero L1 code changes** — diff asserted in review | Contract | planned |
| Metric contracts | [#31](../../issues/31) | Same numbers from all three consoles for one fixture world | E2E | planned |

---
| The Actor Journeys web app reaches the real stack | [#28](../../issues/28) | The browser flow proven end to end against the live stack: page served, CORS preflight answered for the named origin only, token minted, pairwise subject derived, first-login self-proof binding lands (201), authorized party reads answer (200), and the same read anonymous is refused (401). The faces call only endpoints the e2e suite covers. On Railway the same app runs behind one public nginx door that proxies /api/<service>/ over the private network and refuses /internal/* (the §16 fence, verified: healthz 200 through the proxy, /internal/clock 404); seeding runs in-network via tools/seed (`CREST_WORLD` points the fixture loader at the baked-in world, since a distroless container has no repo to walk up) | Spike | spike |
| The gap-closure views read only what the services hold | [#28](../../issues/28) | New views (worker: definition worker-face, notifications; project: payment trace, definition registry, held-payment taxonomy) render solely from GET endpoints the e2e suite already exercises — no client-side fixture data anywhere in apps/web; proven by walking each view against a story-seeded stack and checking every number shown against a direct curl of the same endpoint | Spike | spike |
| Dev login is the real first-login path | [#102](../../issues/102) | `loginAs` in apps/web/api.js binds by token possession through `POST /v1/parties/{id}/identity-bindings` — the same path the harness and a production login callback use; the mock issuer's `/dev/pairwise` exists only where the dev salt does, and a real deployment has no such endpoint anywhere. The journey apps' `loginAs` (apps/shared/api.js) mints the story seeder's own `story\|` subject for the party, so under the narrowed self-proof rule the bind is the idempotent same-subject re-bind, and a bootstrap only on parties the story never bound | Spike | spike |
| A seeded story's payments are readable before the seeder returns | — | `paymentsMaterialised` in `harness/story.go`: after the T=7 exits, the seeder polls until every exit's instruction exists through the outbox relay, and asserts the zero-outcome one is `HELD nothing_to_pay` with its reason — a demo stack whose payments screen races the relay, or a RELEASED zero, fails the seed rather than the first person to open the screen | E2E | covered |
| Story seeding for demo stacks | [#28](../../issues/28) | `SEED_STORY=true go run ./tools/seed` after `make e2e-up`: one coherent week through the real endpoints — consents, a registered + a graded source, two batches, all four T=7 exits (self, dispute, assisted, auto-sweep), a HELD `nothing_to_pay` instruction with an owner, an unclear row, a duplicate hold (409 on resolve), an open recovery, an overdue authorization, two verification-trail entries, and three open windows for a live demo. Re-run detects the registered `riverside-dhis2` source, restores the story clock, and declines; the full e2e suite passes on a storied stack (run on demand, not CI) | Spike | spike |
| The design docs are hosted and shareable | — | Since #148 the Quartz (v4.5.2) rendering of `docs/*.md` — HTML design docs beside it — is a build stage of `infra/railway/Dockerfile.apps`, served at `/docs/` inside the crest-apps door; proven by a local `docker build` + container probe (/docs/, /docs/DEMO, /docs/crest-infrastructure-blueprint.html, /docs/test-manifest all 200; /worker/ still 200; unknown path 404) on 2026-08-29, and by `verify-deployed`'s docs probes once Railway answers again | Spike | spike |
| One infrastructure service: the four members run as one core deployable | #150 | `service.Compose` (pkg/service/compose.go) runs parties, definitions, evidence and verification in one process — per-member schema migration, outbox relay, DeDi set; identity answered in-process by the parties member; readiness fails if any member's store does; route collisions panic at startup. Proven by the full unit suite plus a clean-slate `make test-e2e` (2026-08-29) where every scenario drives the merged core through the same URLs | Unit + E2E | covered |
| Notifications are dropped, and the record stays honest about it | #150 | No enqueue on window open (notified_at stays unset, reach stays NULL — the sweep auto-confirms on NULL, refuses only a recorded unreached); stale outbox rows drain as logged no-ops; `TestARecordBecomesACredentialAndAPayment` asserts notified_at is NOT set, `TestAnUnreachedWorkerIsNotAutoConfirmedAgainst` reshaped to prove the assisted exit still releases payment with no reach verdict; heartbeat scenario keeps detection without a channel. The gap is recorded in Blueprint §14/§16 | E2E | covered |
| The backend services have a roster | — | `docs/SERVICES.md` (#148, reshaped by #150) enumerates the two deployables, the core members, the mocks and the substrate with layer, schema, deployed name and port; a documentation page, proven by reading — it must be updated when the fleet changes, and `verify-deployed`'s `FLEET_SERVICES` sweep is the executable check that the deployed fleet matches | Docs | covered |
| The journey apps render the reference against live services | [#28](../../issues/28) | Four apps (apps/worker, enrolment, console, verify over apps/shared) rebuilt from the Actor Journeys reference with its own design tokens; every screen reads/writes real /v1 endpoints or carries the reference's own "Illustrative" label — no browser-held fixture data; proven by `make e2e-apps` (below) against a story-seeded stack and the deployed door | Playwright walk | spike |
| Every journey-app route stays alive | [#28](../../issues/28) | `tests/e2e-apps/apps.spec.js`: Playwright walks every route of all four apps + landing (9 tests): zero page errors, zero API error banners, story data visible (held payment names its owner, riverside-dhis2 + #117 on Sources, a real logged-out verify round-trip). Run on demand via `make e2e-apps`, locally and with BASE_URL at the Railway door — not in CI (needs a seeded stack) | Playwright | spike |
| Each journey app ships as its own deployment | — | `infra/railway/Dockerfile.app` (APP build arg) serves one app at the domain root with /shared beside it, behind the same allowlisted /api proxy and /internal fence; proven live on all four doors (crest-worker/field/console/verifier-production.up.railway.app): correct title at root, 404 on a non-CREST proxy name, 200 on an allowed one, a real login (4 credentials, 0 JS errors) and a real credential check (Verified) on the standalone doors | Spike | spike |
| Journey apps are mobile responsive | — | `tests/e2e-apps/apps.spec.js` "mobile viewport" — 390px viewport across all four apps: zero horizontal overflow, console sidebar becomes the horizontal chip rail; screens designed in Pencil, implemented as a media-query layer in `apps/shared/crest.css` | make e2e-apps | spike |
| The worker door is a desktop console, rebuilt on React | — | `frontend/` pnpm workspace (React+Vite+TS): `@crest/ui` ports the design system (tokens + console grammar), `@crest/api` ports the fetch/login layer verbatim; `frontend/apps/worker` reimplements the worker routes on the same endpoints (confirm/dispute semantics untouched — every window exit still releases payment); appbar+sidebar on desktop, bottom nav under 720px; built into `Dockerfile.app`/`Dockerfile.apps` and the compose door via `make apps-build`. **Not 1:1 against the reference**: measured W-1 coverage is per-screen in `docs/journey-traceability.md` (see the coverage row below) | Playwright walk | covered |
| The verify door is a desktop console, rebuilt on React | — | `frontend/apps/verify` reimplements the J9 checks (V-1 bare check, V-2 institutional disclosure/batch/person-resolve with the org session held only inside V-2, P-10 panel screens): same endpoints, same route hashes, same test-pinned ids (`#loadsample`, `#verifyform`, `#personform`); tier/table styles ported into `@crest/ui`; wired into both Railway Dockerfiles and the compose door. **Not 1:1**: pass issuance, scan UX, accreditation read and selective disclosure are open — per-screen status in `docs/journey-traceability.md` (V-1, V-2, P-10) | Playwright walk (the pre-existing verify test passes unchanged against the rebuild) | covered |
| The field door is a desktop console, rebuilt on React | — | `frontend/apps/field` reimplements J6 plus assisted confirmation (register/consent/hold/registered, assisted confirm and assisted dispute, evidence-intake roster close with per-row verdicts, handoff — **not J8/W-4 source attestation**, which belongs to the delivery platform; the surface says so, and the W-4 rows in `docs/journey-traceability.md` are `semantically-different`): same endpoints, same `#/registrations`-style hashes, `[data-login]` kept; the offline queue ported verbatim in `queue.ts` (same localStorage key `crest.enrolment.queue`, same record shape) with its count as an appbar chip; served at the same `/enrolment/` path | Playwright walk (the pre-existing enrolment test passes unchanged) | covered |
| The console is a desktop console, rebuilt on React | — | `frontend/apps/console` covers the console read/queue surfaces of J1–J5, J10, J11 and G-4 (#162): role-derived personas with their sidebar groups; project funnel/payments/trace/definition/sources/reports; the definition-authoring wizard (`views/Define.tsx`, 25 reference screens writing real drafts); custodian find/dupes/unclear/recoveries/review and support cases; instance self-description and health sweep — same endpoints, same `#/status`-style hashes, `[data-p]` persona cards and `#logout` kept. The read-only "define work" walkthrough that used to stand in for authoring has been **removed** rather than left beside the real wizard answering the same question differently. **Not 1:1**: project composition and rail setup remain partly read-only — per-screen status in `docs/journey-traceability.md` | Playwright walk (persona walks + the story test) | covered |
| Journey coverage is measured, not claimed | — | `docs/journey-traceability.json` (+ rendered `.md`), generated by `tools/journey-trace/build.py` from the 143 `.role-step` screens of the Actor Journeys reference: every screen carries a status (implemented / compressed / semantically-different / illustrative / missing), the React surface, and the API evidence or the named gap; the script fails if the reference's screen set and the mapping diverge. Current measure: 25 implemented, 23 compressed, 8 semantically-different, 15 illustrative, 72 missing | Generated manifest + review against `docs/JOURNEY_GAP_ASSESSMENT.md` | covered |
| Console personas are role-derived, author/approver separated | — | `frontend/apps/console/src/state.tsx` + `App.tsx`: ten selectable sessions, one per reference role flow (P-1, P-2, P-3 author, P-3 approver, F-1, F-2, G-1, G-4, W-3, V-4); navigation shows only the persona's flow and the route guard bounces any other view; the approver's `#/ratify` (views/Ratify.tsx) now performs the real ratification, naming pending fields. **The separation is no longer only a session contract**: the author persona signs in as the fixture world's specifier party and the approver as the organisation, so the definitions service's own self-ratification refusal applies to them, and the guard is asserted in both directions. Honest limit still recorded in state.tsx: per-role backend *permits* remain unbuilt, so every other org-side role still signs in as the organisation party | `make e2e-apps` — per-persona walks plus "the approver cannot reach the author's wizard", "the author cannot reach the approver's signature" and the two P-3 authoring walks | covered |
| Onboarding asks the reference's six identity fields, as a desktop console frame | — | `frontend/apps/console/src/views/Onboard.tsx` (g2_1): legal name, country, work email ("your domain is taken from this"), contact person, kind, sector — rendered as the reference's desktop console window (appbar, Register→Terms→Certificates→Done rail (every step has a surface since the G-2 screens wave), "Registration · 1 of 4" counter, two-column form grid, "Why so little?" + Continue footer). Registration documents are deliberately NOT asked, per the reference's callout — the suite asserts the field's absence. Since #168 the whole form persists: `POST /v1/organisations` carries the profile in the Party's `attributes` map and the status view reads it back from `GET /v1/organisations/{id}/registration` | `make e2e-apps` — "onboarding asks the six identity fields" + the round-trip test | spike |
| The G-2 org screens are real, frame for frame (g2_5–g2_10, g2_12; g2_8 promoted from compressed) | [#183](https://github.com/theflywheel/CREST/pull/183) | `frontend/apps/console/src/views/OnboardOrg.tsx` + `Onboard.tsx` (`#/onboard/standalone`, `wider`, `documents`, `review`, `invited`, `project`, `checks`): every screen is a real read or write against the parties service AS the registered organisation (the session authenticates via the same first-login bind, never an asserted party id) — the invitation inbox (`GET .../invitations`), decisions and questions (`POST .../decision`, `/questions`), the wider-terms request walk (`POST .../terms-requests` → `PUT .../documents` → `POST .../submit` → `/withdraw`), and checks as recorded verdicts with named owners (`GET .../terms-requests/{id}`). Refusals render honestly: accepting before the registry decided shows the service's own "not yet"; declining without a reason is refused; g2_7 declares `{kind, ref, hash}` references and has NO file input (asserted). Buttons with no journey behind them say so on-screen (People, Assign your people) | `make e2e-apps` — "the G-2 onboarding journey is real, screen by screen" | spike |
| The G-2 walk proves the whole journey end to end | [#183](https://github.com/theflywheel/CREST/pull/183) | `tests/e2e-apps/apps.spec.js`: register → standalone (inbox read, signed-in identity in the appbar asserted, not just an appbar) → invited → accept-before-approval refused with "not yet" → decline-with-reason (recorded, read back) → re-invite → terms-as-request → checks (Certificates step, counter 3 of 4, honestly empty of verdicts) → operator decision (custodian via the real door) → accept → the project with its minted grant and end date → operator publishes a wider set → request → declared documents round-trip → sent for review → withdraw → resubmit → recorded PASS verdict with its named owner shown → approved, and the registration reads back on the requested terms | `make e2e-apps` on a clean `make apps-up` (never a live volume — re-seeding breaks the identity binding, docs/TESTING.md) | spike |
| The reviewer's registration queue is a real read (g4_1) | — | `services/core/parties/onboarding.go` `listRegistrations` + `GET /v1/registrations` (`onboarding_routes.go`): every registration joined to the applicant's self-declared display name and attributes (the same facts the applicant-facing read serves, #168), undecided rows first, oldest waiting applicant on top; `?state=` filters exactly and refuses an unknown state. Authenticated like the terms-request queue — who reviews is L2 routing the service does not encode. `GET /v1/organisations/{id}/registration` now also carries `displayName`, for the same reason it carries attributes | `make e2e-apps` — the G-1 walk reads the queue live; `go test ./services/core/parties` (decision machine unchanged, still covered) | spike |
| The G-1 instance screens are honest frames over the deployment's real self-description (g1_1–g1_3, g1_5, g1_6) | [#185](https://github.com/theflywheel/CREST/issues/185) | `frontend/apps/console/src/views/G1.tsx` (`#/instance/setup`, `covers`, `consent`, `invite`, `services`, `people`) behind the reference's own five-entry G-1 rail: the stand-up front door states that an instance is stood up by compose/Railway and reads `GET /v1/instance` live — no fake wizard, no write; g1_2 renders the published self-description read-only and names the four reference fields `/v1/instance` does not publish rather than inventing values; g1_3 states the consent floor as the infrastructure facts it is (#59 for the words); g1_5 renders the reference's five invitation fields with NO send button — the instance-level invitation has no primitive (#182's offer is a project's) and faking the send would settle g2_5's open entry decision by accident, recorded as design finding #185; g1_6 is the real six-service `/healthz` sweep | `make e2e-apps` — "G-1 walks the instance…" asserts the read-only posture (no instance-name input, "There is nothing to save"), the no-fake-send note naming the finding, and six live health stats | spike |
| Admission review is a real decision with a named decider (g4_1–g4_3) | — | `frontend/apps/console/src/views/G1.tsx` (`#/admissions`, `#/admissions/:pid`): the queue from `GET /v1/registrations` + `GET /v1/terms-requests`, the reference's callouts verbatim; the detail reads the registration with the applicant's declared facts and the "what this does not prove" limit; Approve/Reject goes through `POST /v1/organisations/{id}/decision` with the session's authenticated party as decider — checked by the service against the bearer (#89), never a typed name. Invariant (verification): every approval carries a decider and every rejection a reason — the generalisation of "every held payment has a reason with an owner"; the walk exercises the service's own refusal of a reasonless rejection | `make e2e-apps` — an org registers and accepts terms at the open door, the instance admin (a different party, so the self-approval refusal can hold) walks g1_1→g1_6 by the frames' own buttons, finds the org waiting in the queue, is refused a reasonless rejection, approves, and the record reads back APPROVED with the admin's party as decider; green twice on clean `make apps-up` stacks | spike |
| The Party primitive carries a generic attributes map | [#168](../../issues/168) | `schemas/primitives/party.schema.json`: optional `attributes` — ≤16 keys, bounded string values ≤200 chars; the map is L1, the vocabulary (kind/sector/country taxonomies) stays deployment configuration per the layering test. Proven by `services/core/parties/attributes_test.go`: valid attributes validate, non-string and over-long values are refused, and the public organisation face does NOT carry attributes (self-declared text never reaches the append-only log). Served applicant-facing on the registration read (`onboarding_routes.go`); round-trip proven in Playwright ("onboarding profile round-trips through the registry": submit → sessionStorage holds no profile → status shows the registry-served values). Touches the registration surface only — no evidence/confirmation/payments path changes, no raw identifiers anywhere | Go unit + Playwright round-trip | covered |
| Worker entry: two equal pathways, consent before the record | — | `frontend/apps/worker` Login.tsx presents self-enroll and assisted-enroll as equals (w1_1), the assisted card handing off to the field door; Auth.tsx puts the enrollment-consent step before the signup form (w1_5) and session.tsx records it via `POST /v1/parties/{id}/consents` (moment=enrolment, captureMethod=screen, contextId=the seeded programme) immediately after party creation — the party POST never fires unconsented | `make e2e-apps` — pathway/consent-copy assertions; the consent POST itself rides the eSignet leg, proven by the deployed walkthrough | spike |
| The credential shows as a QR, offline-presentable | — | `frontend/apps/worker/src/screens/Wallet.tsx` CredShow: the signed credential rendered as a QR on-device (`qrcode`, local, nothing leaves the phone), disclosure list unchanged, JSON behind a `<details>` toggle; an oversized credential gets the honest PixelPass gap note (w1_18), never a silent failure | `make e2e-apps` — "show-to-someone renders a QR, JSON behind a toggle" | spike |
| Per-share consent has both faces on screen (w1_19, w1_15, w1_20) | [#190](../../pull/190) | `frontend/apps/worker/src/screens/Shares.tsx` (`#/shares` inbox — requester and purpose BEFORE any disclosure list; `#/shares/:id` — per-line consent against the resolved list, w1_19's callouts, approve-subset or decline-with-reason; `#/shares/:id/sent` — what they got / what you kept from the same `disclosureList` both faces read) + `frontend/apps/verify/src/screens/Requests.tsx` (`#/requests`, org session: create request with a readable purpose, read own requests, collect). Consent invariant asserted structurally: the walk collects BEFORE the decision and asserts the service's 409; after approval it collects exactly the approved subset, once (second collect 409); the decline path records its reason and leaves nothing collectable. Requesters render by proven party id — party reads are self-only (#102), so no name directory exists and none is faked | `make e2e-apps` — "per-share consent: the presentation loop on both faces, and the decline path" | spike |
| Recovery has the worker's and the confirmer's screens (w1_7, w4_1–w4_3) | [#190](../../pull/190) | `frontend/apps/worker/src/screens/Profile.tsx` Recovery (`#/profile/recovery`): party-linked nomination (no tel input on the screen, asserted), revoke keeps the row, self-nomination refused loudly; `frontend/apps/worker/src/screens/Vouch.tsx` (`#/vouch` — the request's content with the SMS-adapter gap stated on-screen (#150, §16); `#/vouch/:id` — live quorum read, N of 2 distinct authorities, completion appends a binding and the old ones stay; `#/vouch/:id/refused` — the reference's "no defined path" questions quoted, then the recorded answers: refusal owned with a required reason, recovery held OPEN, opener named as the next step's owner). The walk drives refusal-without-reason (refused), refusal-with-reason, one YES (1 of 2), a second approved organisation minted through the real open doors vouching for the second confirmer, quorum to CONFIRMED, and completion | `make e2e-apps` — "recovery: nomination routes, a refusal is owned, and two authorities confirm" | spike |
| The no-document worker gets the confidence-check route (w1_4) | [#190](../../pull/190) | `frontend/apps/field/src/screens/Enrol.tsx` Confidence (`#/confidence`, reachable from the register frame's own "No document?" button): `POST /v1/enrolments` with `method=confidence-check` — a provenance fact, never a stored tier; the screen says IA-0-until-anchored in the derived-not-stored words, takes no ID number and no biometric, and the reference's "Add a recovery contact now" works (agent writes the worker's stated party-linked choice, acting for them) before Continue lands on the consent script | `make e2e-apps` — "assisted enrolment: the confidence check records a method, never a tier" (method read back from `GET /v1/parties/{id}/enrolment`) | spike |
| Qualification arrival re-derives earned strength on screen (w1_17) | [#190](../../pull/190) | `frontend/apps/worker/src/screens/Added.tsx` (`#/added`): reads bindings + `GET /v1/parties/{id}/assurance` (derived, with its because) and re-runs the real verifier on demand (`#recheck` → `POST /v1/verify`), showing the tier as derived-at-this-check, stored nowhere. The walk anchors the story's unanchored worker by the first-login binding append (the same path an eSignet anchor takes), asserts the verifier's weakest-assurance caveat was present before (when the stack is clean) and gone after, with the credential untouched and the count unchanged | `make e2e-apps` — "qualification arrival: the anchor lands and earned strength re-derives" | spike |
| #189's promised paths actually execute end to end (three found gaps, fixed) | [#190](../../pull/190) | Driving the wave's screens surfaced three gaps #189's pure-state-machine unit tests could not see: `presentations.scope` CHECK refused `'consented'` (first real collect 500'd — `services/core/verification/migrations/0004_consented_scope.sql`), `assisted_enrolments.method` CHECK refused `'confidence-check'` (first real no-document enrolment 500'd — `services/core/parties/migrations/0015_confidence_check_method.sql`), and the nominate handler authorized with an empty context so the context-scoped field agent could never write the worker's stated choice as its own comment promised (`recoverycontacts.go` now threads `?contextId=` like consent capture) | `make e2e-apps` — the three workers-wave walks each exercise the exact INSERT/authorize that used to fail | spike |
| The legacy apps runtime is retired | — | `apps/shared` deleted (#153 PR5): every door builds from the `frontend/` workspace; the landing page styles itself from `@crest/ui` served as `/crest.css`; `quartz-theme.mjs` mirrors `frontend/packages/ui/src/styles.css`; `apps/` holds only the landing page, the README, and the untouchable PoC (`apps/web`) | Playwright walk (landing card count + zero-overflow) | covered |
| The login is eSignet, server side | [#155](../../issues/155) | `pkg/esignet` (relying-party client: key-derived client id matching the spike's derivation, PKCE, signed state cookie, private_key_jwt exchange) unit-tested in `pkg/esignet/client_test.go`; `pkg/identity` accepts extra issuers (`CREST_OIDC_EXTRA_PROVIDERS`), routing tested in `pkg/identity/multi_test.go`; the parties auth surface (`/v1/auth/login`, `/v1/auth/callback`, `/v1/auth/me`) is in `schemas/openapi/parties.yaml`; the browser leg is proven end to end on the deployed fleet (PIN login → consent → token verified at `/v1/auth/me`, 2026-09-02, after #157/#158/#159) | Unit + deployed walkthrough | covered |

| An authenticated stranger can create their own record (phase B) | [#155](../../issues/155) | Worker door `#/auth` stranger branch → `#signupform` → `POST /v1/parties` (open bootstrap) + self-bind holding the login token (`selfBindAccepted`, #102, already covered by `bindrace`/`identity` scenarios); driven end to end in a browser against the local stack, and by the deployed walkthrough | Browser + existing E2E | covered |
| An organisation onboards itself through the console (phase D) | [#155](../../issues/155) | Console `#/onboard` → `POST /v1/organisations` → `GET /v1/terms` (new listing) → terms-acceptance → state `APPROVED` under `REGISTRY_ORG_APPROVAL=on-terms-acceptance` (configuration, not code); the state machine itself is unit-tested in `services/core/parties`; the flow driven end to end in a browser against the local stack | Browser + unit | covered |
| Certify's data surface serves the confirmed facts, not a fixture | [#155](../../issues/155) | `make e2e-run` — `TestCertifyReadsTheConfirmedFactsNotAFixture`: a claim confirmed through the real window exits into a credential, and `/internal/certify/work-events` then serves that claim's facts for the raw provider subject — every field the WorkEventCredential template renders is present, and the wire carries no `individualId`, no tier, no name, no phone (W9, §6) | E2E | covered |
| An authenticated stranger gets an empty list from the Certify surface | [#155](../../issues/155) | `make e2e-run` — `TestAStrangerGetsAnEmptyListNotAnError`: an unbound subject is a person with no confirmed work here, not a fault; Certify turns the empty list into its own "no data" refusal | E2E | covered |
| Certify issues from the live surface through the CREST plugin | [#155](../../issues/155) | `infra/certify/plugin` loaded by `infra/compose/Dockerfile.certify` and selected by `CERTIFY_DATA_PROVIDER` (default `CrestDataProviderPlugin`); proven by `make certify-issue` against a stack whose Certify runs the plugin — the returned credential's facts are the confirmed claim's, not the CSV's | Spike + deployed walkthrough | covered |
| The whole demo walks in production with no seeded party on screen | [#155](../../issues/155) | `tests/e2e-apps/demo-e2e.mjs` — against the deployed fleet: creates a fresh eSignet mock identity, signs the worker up through the door (`#signupform`), onboards an organisation (`#orgapplyform` → APPROVED), uploads roster evidence for that worker's phone, confirms through the window (fails loudly if no credential issues), and checks the credential at the verifier; then `INDIVIDUAL_ID=… PIN=… make certify-issue` proves the same identity's wallet path, Inji Verify answering SUCCESS | E2E (recorded walkthrough) | covered |
| The design reference has a machine-readable per-screen spec (fidelity Phase 0) | — | `tools/journey-trace/extract-spec.py` (stdlib Python) parses all 143 `.role-step` frames of `docs/reference/CREST — Actor Journeys_17Aug.html` and emits `docs/journey-spec.json` — per screen id: journey, actor, stage, title, level badge, layout idiom (desktop/phone), fields (label+hint+control kind), callouts, buttons (label+role), step counter + rail, appbar text, and a sha256 fingerprint of the frame's normalized visible text so reference drift is detectable; deterministic (sorted keys, document order — same input, byte-identical output). `make journey-spec` regenerates; `make journey-spec-check` (CI docs job, same generate-and-diff contract as `generate-check`) fails on a stale committed JSON. Phase 1's per-screen Playwright assertions consume this spec (row below); since Phase 1 the file also carries the five J3 connective-tissue screens `n1`–`n5` from `tools/journey-trace/crest-design-screens.json`, marked `source: "crest-design"` so our design is never attributed to the reference | Generated-artifact staleness gate (CI) | covered |
| A screen is held to the reference, screen by screen (fidelity Phase 1, J3) | — | `tests/e2e-apps/fidelity.spec.js` + `make fidelity`: for every screen in `tests/e2e-apps/fidelity-map.json` (scope today: J3 — `p1_*`, `p2_*` — G-2 org onboarding — `g2_*` — G-1's instance walk and admission review — `g1_*`, `g4_1`–`g4_3` — the workers wave's nine — `w1_4/7/15/17/19/20`, `w4_1`–`w4_3` — plus the design screens `n1`–`n5`) the suite drives the real story-seeded stack to that screen and asserts its `docs/journey-spec.json` entry — every reference field label, every callout title and callout body in full, the primary/secondary button labels, the step counter, every rail entry, the layout idiom (a desktop-idiom frame must render appbar + rail, not a phone card), and **forbidden content**: what the reference deliberately withholds must be absent (`tests/e2e-apps/fidelity-forbidden.json`, the g2_1 registration-number class of bug; design screens carry their own list). Three verdicts and no fourth — *asserted* (status `implemented`, every unwaived facet held), *skipped with the ledger's own reason* (status not `implemented`), *quarantined* (claimed implemented, unreachable today, `fidelity-quarantine.json`, must name an issue, and FAILS if it starts passing). Unheld facets live in `fidelity-waivers.json`, each with the reason and the unbuilt design behind it — a waiver is counted debt, not a loosened assertion. Extending to another journey is adding routes, not editing the gate. **What it cannot judge**: spacing, weight, colour, alignment, rhythm — anything visual (see the contact sheet row) — and reference titles/appbar identities that are fixture data rather than design | `make fidelity` against `make apps-up` (currently 52 asserted, 0 quarantined, 16 skipped-with-reason, 0 failed, 50 waived facets across 68 screens in scope — twice on one clean stack, so the arrivals are re-runnable) | spike |
| The ledger cannot claim coverage the gate denies | — | `tools/journey-trace/fidelity-ledger.py`, run by `make fidelity` straight after the suite: reads the gate's per-screen verdicts (`tests/e2e-apps/fidelity-results.jsonl`, gitignored) and fails when a screen statused `implemented` failed its assertions, when an in-scope screen produced no verdict at all (unjudged is not passed), or when a waiver/quarantine carries no reason, names no issue, or points at a screen the ledger no longer calls `implemented`. The **Gate** column in `docs/journey-traceability.md` is generated by `tools/journey-trace/build.py` from the gate's own scope file plus the ledger's statuses, so scope and ledger cannot drift; the column says which screens are asserted, never that they passed — only a run says that | `make fidelity` (the check is the last step and fails the target) | spike |
| The fidelity gate reaches G-2 — onboarding held to the reference | — | `tests/e2e-apps/fidelity-map.json` adds the G-2 journey (`g2_1`–`g2_13`; `g2_2`/`g2_3` do not exist in the reference) to the gate's scope as DATA ONLY — no gate-logic change, which was the design of the gate. `g2_1` arrives `anon` (the onboarding door is deliberately reachable with no persona — #20, #155 phase D) and is asserted in full — six field labels, both callouts, both buttons, `Registration · 1 of 4`, the Register/Terms/Certificates/Done rail, and the pre-existing forbidden rule (no registration-number field) — with one waived clause of callout copy. Every screen past it (`g2_4`–`g2_13`) arrives by a `flow:` mode (row below, #179) and is asserted; the extractor-defect callout waivers on `g2_4`/`g2_5`/`g2_11` (bolded lead-ins moved after their sentence by `extract-spec.py`, the p2_14 defect) stand, plus the real copy gaps the flow arrivals made measurable on the shared status screen — g2_4's missing 'Terms and projects are different things' callout, g2_13's missing 'One thing still not unlocked' callout and 'Copy the key' control — each waived as unwritten copy / a missing control, never loosened. The ledger's Gate column and scope sentence regenerate from the map (`tools/journey-trace/build.py`) | `make fidelity` against a fresh `make apps-up` (g2 scope: all 11 asserted, 0 quarantined, 0 skipped) + `make fidelity-check` | spike |
| The fidelity gate can arrive mid-flow — scripted-flow arrivals for session-scoped screens | #179 | `tests/e2e-apps/fidelity.spec.js` grows one arrival family, `flow:<name>`, in the MAP's vocabulary (`tests/e2e-apps/fidelity-map.json`) — per-screen data, never per-screen gate edits. A `flow:` arrival drives the real onboarding flow against the real stack before landing on the route — the g2_1 register form (a unique organisation per arrival, so the run is re-runnable on a live stack), then as deep as the screen needs: terms requested (`flow:onboarding-terms-request`), the operator's real decision as the custodian (`flow:onboarding-approved`), a project's real invitation via POST /v1/projects/{id}/invitations as the fixture org (`flow:onboarding-invited`), acceptance minting the grant (`flow:onboarding-project`), a wider published set + open terms-request (`flow:onboarding-wider-request`), documents declared + submitted (`flow:onboarding-wider-submitted`), and a second published set so the terms catalogue has other sets to name (`flow:onboarding-terms`). The out-of-band acts use the same bearer-token doors as `apps.spec.js`'s G-2 walk and the seeder; the flow steps mirror that walk (which remains the proof of the flow itself — the gate only arrives by it). Emptied `fidelity-quarantine.json`: all ten #179 entries (g2_4–g2_13) deleted and those screens asserted | `make fidelity` (three consecutive runs on one stack locally — re-runnable) + `make fidelity-check` + CI journey-doors job | spike |
| The fidelity gate reaches G-1 — the instance walk and the admission review held to the reference | [#187](../../pull/187), [#185](../../issues/185) | `tests/e2e-apps/fidelity-map.json` adds the G-1 journey as DATA plus one new flow depth pair: `g1_1`/`g1_2`/`g1_3`/`g1_6` arrive `console:instance` and cold-load their `#/instance/*` routes (they read deployment state, no session), all asserted; `g1_5` stays `compressed` (design finding #185 — no instance-level invitation primitive) and skips with that note. `g4_1`–`g4_3` arrive by `flow:admissions-queue` / `flow:admissions-review` (`fidelity.spec.js`): the arrival registers a unique organisation through the real open door, signs in as the instance administrator through the persona card, and lands on the real queue / that registration's own `#/admissions/:pid` — the map's route stays `''` because the pid is per-run and the console token is in-memory (a post-flow reload would sign the reviewer out). Three waived facets, each a named gap: g1_3's `Save and continue` (nothing to save — #59), g4_2's `Work email domain` field (no domain check exists) and `Approve the request` label (the control is `Approve and publish`, asserted end to end elsewhere). The rest of g4 (`g4_4`–`g4_7`) stays out of declared scope — the map's `why` says why | `make fidelity` twice on one clean `make apps-up` stack (32 asserted, 0 quarantined, 16 skipped-with-reason, 0 failed) + `make fidelity-check` | spike |
| The fidelity gate reaches the workers wave — nine screens held to the reference | [#190](../../pull/190) | `tests/e2e-apps/fidelity-map.json` adds the nine screens #189/#190 flipped as picked entries (the rest of w1 predates the gate and stays its own piece of work): `w1_7` arrives `worker`, `w1_4` arrives `field` at `#/confidence` (every asserted facet renders before submit), and the rest by new worker-door `flow:` depths in `fidelity.spec.js` — `flow:share-pending` (the verifier org's real presentation request, opened by its subject with two lines left ticked), `flow:share-decided` (decided by the subject's own bearer voice, landing on the sent view), `flow:recovery-confirmer`/`-progress`/`-refused` (nomination + an OPEN recovery through the real registry doors, a recorded refusal for the refused depth, viewed as the signed-in supervisor), `flow:anchor-arrival` (the story's unanchored worker's first dev sign-in — the binding append — landing on `#/added`). All nine asserted; twelve waived facets, each named: the worker/field doors serve the reference's phone frames in the console idiom (layout), the reference's console-mock appbar reads 'CREST Console' where the doors honestly name themselves (appbar), w1_7's step counter belongs to the enrolment flow the profile surface is not, and the two live-count callouts (w1_19's 'four lines', w1_20's 'twelve/nine') render real counts and name the reference's numbers as its worked example | `make fidelity` twice on one clean `make apps-up` stack (41 asserted, 0 quarantined, 16 skipped-with-reason, 0 failed, 50 waived facets across 57 in scope) + `make fidelity-check` | spike |
| The J2 recording walks the built G-1 screens, not the old compressed `#/instance` | [#187](../../pull/187) | `tests/e2e-apps/journeys.mjs` J2: seeds a real pending registration through `POST /v1/organisations` (unique per run), signs in as the instance administrator, and walks setup → covers → consent → invite (naming finding #185, no faked send) → services → the real admissions queue → the review detail → an actual approval through `POST /v1/organisations/{id}/decision`, every caption stating what is live and what is deploy-time configuration | `OUT=… node tests/e2e-apps/journeys.mjs J2` against `make apps-up` — "journey ok: J2" (the .webm is local, never committed) | spike |
| The J7 and J10 recordings walk the workers wave instead of naming it missing | [#190](../../pull/190) | `tests/e2e-apps/journeys.mjs`: J7 replaces the "share links — the honest gap" and "recovery — nomination is the honest gap" captions with the real consent loop (a request minted through the verification service, opened, one line unticked, the subset approved, the sent view with live history counts) and the real nomination surface, and adds the `#/added` re-derivation moment (w1_16 stays a named gap); J10 keeps the custodian view and then crosses to the confirmer's own `#/vouch` screens as the signed-in supervisor (nomination + open-or-reuse recovery through the same bearer doors the walks use), the SMS channel gap stated on-screen | `OUT=… node tests/e2e-apps/journeys.mjs J7 J10` against `make apps-up` — "journey ok: J7", "journey ok: J10" (the .webm is local, never committed) | spike |
| The visual grammar gets a human's eye, deliberately | — | `tools/journey-trace/contact-sheet.mjs` + `make fidelity-sheet`: renders `docs/reference/CREST — Actor Journeys_17Aug.html` in the same browser, screenshots each `#scr-<id>` frame, shoots the implemented screen beside it, and writes `docs/.fidelity-sheet/index.html` with the pairs, each labelled with status, source and the ledger's gap note. Design screens (`n1`–`n5`) show "no reference frame — our own design". Output is gitignored: a PNG in git is a fidelity claim nobody re-derives. This is the honest complement to the gate — assertions cannot see rhythm, and a screenshot diff would go red on every fixture change | Human review (by construction — no assertion) | spike |

| The J3 console screens carry the reference's own frames | — | `frontend/apps/console/src/views/Setup.tsx`, `Dashboard.tsx`, `SignIn.tsx`: the P-1/P-2 frames rebuilt to the 17 Aug reference — the reference's titles, ledes, option copy, field labels and callout text, over live service reads (parties/registration/authorizations, claims, unclear rows, unreleased windows, instructions, sources, source-assessments, definitions). Held to `docs/journey-spec.json` by `make fidelity`: `p2_11`, `p2_14`, `p2_15`, `p2_21` asserted with 25 waived facets (was 42 — every rail waiver on the dashboard frames is deleted, and the callout waivers are now per-callout so the ones carried verbatim are asserted). Screens whose decision has nowhere to be stored (`p1_3`, `p2_2`, `p2_4`, `p2_9`, `p2_10`, `p2_19`) render the reference's frame in full with an honest note naming the missing store, and stay statused `compressed`/`illustrative` in `docs/journey-traceability.md` | `make fidelity` + `make e2e-apps` | spike |
| The console rail is the reference's, per section, and never hides an entry | — | `frontend/apps/console/src/App.tsx` `railFor`: the three rails the reference actually draws across the 24 J3 frames (setup · dashboard · finance-support), identical for both J3 actors, with every route held by both — so no entry is removed by role. Proven by `tests/e2e-apps/apps.spec.js` "the J3 rail is the same five entries for both actors" (rail contents compared entry-for-entry for orgadmin and configurator, then again after switching to the dashboard section) and "People & roles guards the configurator instead of hiding" (the entry is present, the route renders the guard rather than redirecting, and no HTTP status code is shown). Records the F1 correction: the reference's rail is identical per section, not across all of J3 (`docs/design/j3-connective-tissue/README.md`) | Playwright (`make e2e-apps`) | spike |
| The console door offers no role to pick (n1) | — | `frontend/apps/console/src/views/SignIn.tsx`: eSignet primary through the same server-side flow the worker door uses, demo personas only where the mock issuer's discovery document answers, and no role selector of any kind. `tests/e2e-apps/apps.spec.js` "console door: eSignet is the way in, and no role can be picked" asserts the design spec's rule verbatim and that the page carries no `select`, no radio and no control named for a role — a role is granted in the registry and read back, never self-declared | Playwright (`make e2e-apps`) | spike |
| A console persona walk proves arrival, not just an appbar | — | `tests/e2e-apps/apps.spec.js`: the logged-out door renders an `.appbar` too, so the persona walk now waits for `#logout`, asserts the signed-in identity in the appbar, asserts the door's sign-in title is gone, and re-checks all three plus the URL after every route — a broken login or a guard that bounced the walk home can no longer pass. Found while measuring this wave; the old assertion would have gone green on a 403 sign-in | Playwright (`make e2e-apps`) | spike |

| The J3 handover is acknowledged, and a decline is a real outcome | [#173](../../issues/173) | `frontend/apps/console/src/views/J3.tsx` + `Setup.tsx` over the project endpoints: `POST /v1/projects` creates DRAFT with ownership PENDING, `POST .../ownership-decision` accepts or declines with a required reason, `POST .../configurator` re-hands, `GET .../ownership` renders the whole append-only trail. `tests/e2e-apps/apps.spec.js` "the J3 handover is real, and so is everything after it" drives create → decline with a reason → the Org Admin's declined queue → re-hand → accept through the screens and asserts each fact read back from the service — including that the decline's reason and both trail events survive the re-handover (design finding F2) | Playwright (`make e2e-apps`) | spike |
| Configuring requires an accepted handover, and says so instead of hiding | [#173](../../issues/173) | The 409 `handover_not_accepted` is rendered as a boundary, not an error: `WriteRefusal` in J3.tsx states that the project stays readable and names the organisation that can re-hand it. Viewing works in any ownership state, which is why the handover screen itself is reachable before any grant exists | Reviewed against the endpoint contract; the accepted path is proven by the walk above | spike |
| The project's composition, roles, gates, finance link and support owner are records, not browser state | [#173](../../issues/173) | Composition (`PUT .../composition/{choice}`), project roles (`POST|GET .../roles`), activation (`GET|POST .../activation`, `PUT .../activation/gates`, `POST .../gates/{name}/satisfied`), finance link (`PUT|GET .../finance-link`) and support owner (`PUT|GET .../support-owner`) are all written and read back through the screens in the same Playwright walk: a composition choice appears with its decider, a role grant with its grantor and state, an unmet gate refuses activation by name and the project reaches ACTIVE only after it is satisfied, a support owner who is not a Party is refused, and a finance code is stored verbatim. No taxonomy is hardcoded in the UI — choice names, values, gate names and role functions are all L2 vocabulary the screens take as given | Playwright (`make e2e-apps`) | spike |
| The partner directory is a join over approvals, and a partner grant must end | [#173](../../issues/173) | `GET /v1/partners` filtered by sector, country and what the terms allow, plus `POST|GET .../partner-grants` on the same screen. The walk asserts the approved programme organisation appears in the directory, that a grant with no end date is refused (`422 grant_must_end` — the row the backend PR marked unproven), and that a grant with one stands and lists with the terms version it rides. The invitation the reference sends next does not exist and the screen says so | Playwright (`make e2e-apps`) | spike |
| A browser can call the J3 configuration endpoints at all | [#173](../../issues/173) | `pkg/httpx/cors.go` now allows `PUT` in `Access-Control-Allow-Methods`. #173 shipped five PUT endpoints with no browser path to reach them: the preflight failed and every door saw "Failed to fetch", which the service-level tests could not notice because they call the service directly. The Playwright walk above exercises four of those PUTs through a real door, so the gap cannot silently reopen | Playwright (`make e2e-apps`) | spike |
| The fidelity gate reaches the funders wave — eleven F-1/F-2 screens held to the reference | [#193](../../pull/193) | `tests/e2e-apps/fidelity-map.json` adds `f1_2`–`f1_5` and `f2_4`–`f2_10`: `f1_2` cold-loads `console:orgadmin` at `#/rateowner` (the assignment form renders before any assignment exists); the rest need real state behind them and arrive by new `flow:` depths in `fidelity.spec.js` — `flow:funders-rate` (the org self-assigned as owner, signed in as the rate owner), `flow:funders-rate-published` (priced and published, landing on whichever of `#/ratepublish`/`#/ratestanding` the entry names), `flow:funders-mechanism` (a fresh project, the payment mechanism owner signed in, a mechanism created on it — landing on whichever `mech/*` screen the entry names), `flow:funders-held` (a real batch confirmed by the worker's own exit while the mechanism sits configured-not-live, landing on the invariant screen `#/mech/qualify` with a real HELD `mechanism_not_live` instruction on it) and `flow:funders-live` (every activation condition completed for real — test disbursement, recon agreement, statement agreement, batching choice, qualification submitted and verified — then activated, landing on `#/mech/live`). These take their own route rather than the generic post-flow reload, same reason as `flow:admissions-*`: the console's persona token and its selected project both live in in-memory state, which a reload erases. All eleven asserted, zero waivers needed — the reference's own copy on these frames matched the built screens field for field, including f1_2's "Decline the request"/"Accept and invite" reference buttons, which the assignment screen's own act satisfies literally | `make fidelity` twice on one clean `make apps-up` stack (52 asserted, 0 quarantined, 16 skipped-with-reason, 0 failed, 50 waived facets across 68 in scope) + `make fidelity-check` | spike |
| The fidelity gate can see our own design screens | — | `tests/e2e-apps/fidelity.spec.js` builds its status map from `LEDGER.rows` **and** `LEDGER.designRows`, so `n1`–`n5` are asserted when the ledger calls them implemented instead of always reporting `skipped (designed)`. Four of the five are now asserted (n1, n2, n3, n5); n4 stays `compressed` because the fixture world seeds no pending handover for it to render | `make fidelity` | spike |
| The tail wave's four registry/receipt screens read the real endpoints, not a completeness chart (g4_4, g4_5, g4_7, w6_3) | [#197](../../pull/197) | `frontend/apps/console/src/views/Custodian.tsx` (`Coverage`, `QualityWorklist`, `RegistryReuse`) and `views/Project.tsx` (`Receipt`), wired at `#/coverage`, `#/registry-quality`, `#/reuse` (custodian G-4 rail) and `#/receipt` (instance/project rail). g4_4 reads `GET /v1/coverage?attribute=` with a caller-supplied attribute key and renders the honest `unspecified` bucket rather than a fabricated percentage; g4_5 reads `GET /v1/quality-worklist`, a row per party with its named gap and who can fix it, silent for a clean party; g4_7 reads `GET /v1/registry-reuse` with its derivation string shown on-screen and the `reuseRate: null` state rendered as "—", never 0. w6_3 ("What the project received, and where it sits") is grouped under P-10 in the reference but is a project-side read — the project that requested evidence sees what arrived, not the external institution that sent it — so it lives beside `Definition`/`Sources` in the project rail rather than in the verify door's panel shell where the still-illustrative w6_1/w6_2 stay; it looks up a batch id and reads `GET /v1/batches/{id}/receipt`, rendering the batch, its units' ingestion state per claim, and any unclear rows with their open-queue position. Callouts are the reference's own text verbatim, reordered metric-name-first to match the established "Guarded by ·" convention (Dashboard.tsx). Invariant: all four are read-only over existing records — no write path, nothing that touches confirmation or payments | Playwright (`make e2e-apps`) — the custodian persona walk now cold-loads all three metric routes; a dedicated test drives them for real content (total registered, the unspecified bucket, the parties-with-gaps count, the reuse derivation text), submits a real batch via `POST /v1/batches`, and reads its receipt back as the project persona, asserting the batch id, an outcome unit and a real claim state render, all while asserting signed-in identity; green twice on one clean `make apps-up` stack | spike |
| The P-3 wizard carries the reference's density: an 820px content pane, a right-aligned Back/Continue pair, and a real progress track | — | `frontend/packages/ui/src/components.tsx` adds `WizardContent` (the `.wizard-content` max-width wrapper) and `ProgressBar` (`.progressbar`/`.fill`, an orange fill on the divider-grey track); `frontend/apps/console/src/views/Define.tsx`'s shared `Frame` wraps every gated `/define/*` step in `WizardContent` and derives the track's value/max from the step counter string each screen already sets (`"Sector · 1 of 9"` → 1/9), rendering it beside the unchanged `.eyebrow` `StepCounter` rather than replacing it. `Frame`'s new `wide` escape hatch keeps the registry front door (`#/definework`, a list screen) at full width — the one `Frame` caller that opts out. No classname pinned by `tests/e2e-apps/apps.spec.js` was renamed. Invariant: cosmetic only — no evidence, confirmation, payments or verification path touched | `pnpm -r build` + `pnpm -r typecheck` (frontend workspace); `make apps-build` + `tests/e2e-apps/apps.spec.js` (blocked in this session by a pre-existing environmental failure unrelated to this diff — see PR body) | spike |
| After a real eSignet sign-in the console appbar shows the person's own name, not a placeholder | — | `frontend/apps/console/src/state.tsx` adds `completeEsignet(token)` — the console had no eSignet return leg at all before this change (only the worker app did); `frontend/apps/console/src/App.tsx` adds the `/auth` route (`AuthReturn`) the server's `/v1/auth/callback` already redirects to. On completion it calls the existing `GET /v1/auth/me` and `GET /v1/instance` (role: instance operator or a default Org Admin — the per-party authorization self-read this would otherwise derive from is L1 work still on its own branch, so this is an honest, named limit, not a silent guess) and then reads the party's own `displayName` via the existing self-readable `GET /v1/parties/{id}`, using it for `who`; a failed or empty read falls back to the literal "Signed in via eSignet". No new backend surface — every endpoint called already exists and is already a person's own self-read | `pnpm -r build` + `pnpm -r typecheck`; not covered by an automated walk yet (the local stack has no real eSignet-issued token to drive one against) — reviewed against `services/core/parties/routes.go`'s `getParty`/`getInstance`/`/v1/auth/me` handlers for the self-read authorization each relies on | spike |

| The doors' entry screens carry no dev sign-in — programmatic session hand-off keeps the suites honest | — | Worker `Login.tsx` and console `SignIn.tsx` offer only the real pathways (eSignet; the field-door handoff); the fixture-persona cards, the party-id form and the console demo block are deleted, not hidden. The suites and the journey recorder sign in the way those controls worked underneath — a mock-issuer token, the same idempotent `POST /v1/parties/{id}/identity-bindings`, the session written to the door's own sessionStorage key (`crest.worker.session`, `crest.console.session`) — via `workerSignIn`/`consoleSignIn` (`apps.spec.js`), `flowWorkerLogin`/`flowConsoleLogin` (`fidelity.spec.js`) and the recorder's twins (`journeys.mjs`). `apps.spec.js` "console door" asserts the persona panel is absent. The console session now persists across a reload (it was memory-only) | Playwright (`make e2e-apps` + `make fidelity`) on the next clean stack — rewired this wave, not yet run green as a suite | stated |
| The console has an eSignet return leg (#155) | — | `frontend/apps/console/src/views/SignIn.tsx` `AuthReturn` + `state.tsx` `completeEsignet`: `#/auth` renders before the signed-out gate, verifies the token via `whoAmI`, and a verified identity bound to no party gets the honest "you hold no role here" screen — the console has no self-registration. Honest limit restated in code: per-party role permits are missing (p1_2), so a real sign-in lands on the Org Admin view rather than a registry-derived one | Manual local walk (302 → eSignet UI → callback proxied); no suite drives the redirect round-trip yet | stated |
| The local stack runs the wallet leg and the eSignet login leg prod-faithfully | — | Compose gains `mimoto` (Dockerfile.mimoto, own `inji_mimoto` database, keystore volume), a rebuilt `inji-web` (local nginx upstream, stock env-config generation restored — the entrypoint previously skipped it and shipped the baked `localhost:8099` default, latent on Railway too), `esignet-ui` on 58088 fronting the API (p0 C12, the raw API moved to 58089), a socat sidecar in Mimoto's netns so the browser-facing eSignet URL resolves in-container, and the Railway door nginx route policy on the local `apps` server (`infra/compose/nginx.apps.local.conf`) including door-prefixed `/api/` for the login callback. `injiweb-start.sh`/`mimoto-start.sh` gained a Railway-inert local-config copy step | Manual local walk: wallet issuers 200 with substituted client id, eSignet well-knowns 200, login 302 with browser-reachable authorize, callback answers core's 400 on a bare GET. No automated suite covers the wallet leg | stated |
| The OIDC clients register a logo that exists | — | `frontend/static/logo.png` (the design system's mark as a real asset), served from the door root by `assemble.mjs`; `pkg/esignet` `Client.LogoURI` from `CREST_LOGO_URL`, `mimoto-onboard.py` reads the same variable; the old URL (`crest.theflywheel.in/logo.png`) does not resolve, so this is broken on Railway today — and eSignet will not update a registered client's logo, so fixing a deployment means a new client (C9 consequence, named for the deploy decision) | `go build` + manual: `/logo.png` answers 200 `image/png`; fresh local clients registered with it | stated |
| The console reads a real world, not fixture ids — and `GET /v1/definitions` exists to make that possible | — | `services/core/definitions` gains a list endpoint (one entry per id, the version a resolver would follow — newest ACTIVE else newest; `?state=`/`?limit=`; blueprint §13 updated in the same change). Console `state.tsx` loads the signed-in party's projects (`GET /v1/projects?ownerPartyId=…`, falling back to `configuratorPartyId`) and the definitions list on sign-in, exposing `projects/projectId/definitions/definitionId`; every view that hardcoded `FIX.org`/`FIX.project`/`FIX.definition` (Admin, Dashboard, Project, Setup, Ratify, Custodian, J3) now reads state, with honest empty states on a clean slate. Fixture sessions pass through the same queries, so the seeded suites read identically | `go test ./services/core/definitions`, `tsc --noEmit`, live local walk on a clean slate (list answers `[]`, console renders empty states, no `party_not_proven`); the seeded read is CI's `make e2e-apps` on this PR | stated |

| The definition vocabulary is declared through the product, and the wizard renders it as the reference's cards | — | Self-serve L2 vocabulary, no seeds: the configurator console gains `#/vocabulary` (`frontend/apps/console/src/views/J3.tsx` `Vocabulary`), writing `PUT /v1/projects/{id}/composition/definition-vocabulary` — the same accountable composition write path as p2_1/p2_3/p2_5, so the record carries who declared the words and when. The wizard's `useVocab` reads the composition record (falling back to creation-time `configuration.definitionVocabulary`), and a declared list renders as the reference's card grid (`.vocab-cards`/`.vcard`, entry format `value | descriptor`; the value is what `classification.sector` records). Enabling the read: `gateProject` now admits READS by a party holding an ACTIVE authorization scoped to the context (`grantAdmitsRead`), because an author could not read the vocabulary their own wizard offers; writes are unchanged | `go test ./services/core/parties` (`TestAGrantScopedToTheContextAdmitsReadsAndNothingElse` — scoped grant admits, other/unscoped/empty refused), `tsc --noEmit`, live local walk end to end: Org Admin declares on the project, the granted author's Sector screen renders the seven cards and picking one records the value; no Playwright walk drives the vocabulary screen yet | stated |
| A person reads their own grants, and the console derives P-3 from them | — | `GET /v1/authorizations/mine` (parties): the #68 self-read — a signed-in caller lists the ACTIVE grants they hold, both scopes; unauthenticated callers are refused, and the third-party listing stays organisation-only. The console's eSignet sign-in derives the author/approver personas from the caller's own functions (`specify-definition` / `ratify-definition`) and world-loads the projects those grants name when the party owns and configures none | `go test ./services/core/parties` (existing auth/store suites), live local walk: a granted author signs in via eSignet and lands on the authoring rail; a dedicated handler test is still owed | stated |

## Known gaps

Recorded rather than quietly carried:

- **The spec's callout text can carry a word order no screen can match.** `tools/journey-trace/extract-spec.py` collects a node's own text before its children's, so a callout whose first words are a bolded metric name comes out with that name moved after the sentence it opens (`p2_14`: "looks better if slow records are excluded… Confirmed-paid percentage Guarded by …"). The fidelity gate therefore cannot assert those bodies, and `tests/e2e-apps/fidelity-waivers.json` records it as an extraction defect rather than a copy gap. The fix is in the extractor, not in the screens.
- **The activation read cannot tell a declared gate from an infrastructure condition.** `GET /v1/projects/{id}/activation` returns both in one list, and `PUT .../activation/gates` replaces the declared list — so a console cannot echo the list back without promoting "the handover is acknowledged" into a deployment gate somebody could tick by hand. The console sends only the gates declared from the screen and says so on the screen; the honest fix is a flag on each condition saying which kind it is.
- **No pending handover exists in the fixture world.** `n4` renders the receiving side from a real record, but a fresh stack has no project anybody has been handed, so the screen correctly says there is nothing to accept and the fidelity gate has no accept/decline state to assert. It stays `compressed` until the story seeds a handover.

- **Nothing in the product is `covered` yet** — only the foundations, the P0 spikes and the registry-publication rows above. This file is the specification of proof, written before the thing it proves, which is the intended order.
- **A `spike` row is not a regression test.** Nothing re-runs `make spike-dedi` automatically, so a DeDi upgrade could break an assumption here without anything going red. #20 has now put DeDi behind a CREST interface with tests of its own, which closes half of this: the wire contract and the pin check are `covered`. The half that remains is that **nothing in CI talks to a real node.** CI runs the Postgres fallback, because the node needs publisher keys minted from a checkout. The node path is `make verify-registry`, run by hand.
- **A published fact is checked for inclusion, never for consistency.** An inclusion proof says a record is in *a* log; a consistency proof says the log was never rewritten, which is the property an independent verifier actually depends on. The node exposes `/dedi/log/proof/consistency` and nothing in CREST reads it yet. Carried over from the P0 spike's "not covered by this spike" list, and still true.
- **One P0 claim cannot be settled locally at all**: offline verification (#1) needs a device with its radios off. Pairwise subject identity (#3) was listed here too and that was wrong twice over: self-hosted eSignet runs the same code a sandbox runs, so it answered the question; and "production access" was never a sandbox at all but a relying-party registration by a country's identity authority, which is [#53](../../issues/53) and waits on a pilot geography.
- **Offline verification (W6) needs real hardware.** A container asserting it has no network is weaker evidence than a phone in a field with no signal. The plan schedules a field simulation in week 12; W6's status stays `partial` until that happens, however green CI is.
- **The skill list is deployment-local, and a skill code exists to be shared.** §3 puts the canonical list on a global DeDi node that does not exist ([#76](../../issues/76)), so two deployments can mint the same code meaning different things and nothing would notice. Shipped anyway because the codes are stable and in credentials from day one, which makes adopting a shared list later a mirroring problem rather than a reissuance one.
- **One fact, two suites, two verifiers ([#164](../../issues/164)).** The wallet credential (Certify) signs `Ed25519Signature2020` because Inji Verify implements no DataIntegrityProof suite; CREST's credentials keep `eddsa-jcs-2022` for offline verification. So CREST's `/v1/verify` cannot check a wallet credential and Inji Verify cannot check a CREST one. Each is verified by its own stack's verifier; convergence is recorded in the finding, not half-built.
- **The wallet credential is the newest confirmed work event only.** The OpenID4VCI flow has no record-selection step, so Certify issues over the newest confirmed claim; an older event cannot be pulled into the wallet yet. Named on the worker door's wallet screen.
- **An offline verifier cannot see a dispute.** A contest is resolved at verification time from the deployment that holds it, so a bare QR scan shows a valid credential with no indication it is contested ([#58](../../issues/58)). The status list has the same shape and §5 already accepts it; publishing contests would fix it and is refused because a public log of disputed claims is a public record of which workers are in dispute ([#68](../../issues/68)).
- **An unregistered source is not watched.** Heartbeat monitoring only covers feeds somebody registered with a declared cadence. Auto-registering on first sight would mean inventing a cadence, and an invented cadence produces alerts nobody believes — which is worse than no alert, because it teaches people to ignore the real one. The consequence is that a feed nobody registered can stop sending and nothing will say so.
- **Rail behaviour is only ever proven against a sandbox** until production. Sandboxes lie about failure modes — treat the first real payment run as a test, with someone watching.
