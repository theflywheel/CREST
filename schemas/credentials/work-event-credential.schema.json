{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:crest:schema:credentials:work-event-credential:1",
  "title": "WorkEventCredential",
  "description": "The credential a worker holds (§5). It carries provenance facts and never a tier: the tier is computed at presentation time against the pinned definition version, so re-assessing a compromised source downgrades every affected credential instantly without reissuance (§6).",
  "type": "object",
  "properties": {
    "@context": {
      "type": "array",
      "minItems": 2,
      "items": {
        "type": "string"
      },
      "description": "W3C VC context plus CREST's own."
    },
    "id": {
      "type": "string",
      "minLength": 1
    },
    "type": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 2,
      "description": "['VerifiableCredential', 'WorkEventCredential']"
    },
    "issuer": {
      "type": "string",
      "minLength": 1
    },
    "validFrom": {
      "$ref": "urn:crest:schema:primitives:common:1#/$defs/timestamp"
    },
    "credentialSubject": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "The pairwise subject reference. Never a national identifier, and never a name (W8, W9)."
        },
        "workEvent": {
          "type": "object",
          "properties": {
            "claimId": {
              "$ref": "urn:crest:schema:primitives:common:1#/$defs/id",
              "description": "The accepted Claim this credential projects.\n\nDistinct from `eventId`, and the distinction is the object model's: a Unit says the work happened, a Claim says who performed it. A credential is a projection of the Claim, so naming only the Unit would leave the credential unable to say which of several claims on that Unit it is about — and a disputed claim must never destroy the underlying Unit, which only works if the two are separately addressable."
            },
            "eventId": {
              "$ref": "urn:crest:schema:primitives:common:1#/$defs/id"
            },
            "definition": {
              "$ref": "urn:crest:schema:primitives:common:1#/$defs/versionedRef"
            },
            "definitionProof": {
              "type": "object",
              "description": "Where the definition version this credential was measured under can be resolved, and the digest it had when this credential was issued (#16, Blueprint §3 and §5).\n\n`definition` above is the reference; this is what makes it provable. Without it a verifier asking \"what did this definition actually say?\" can only ask the issuer, and an issuer confirming their own definition establishes nothing.\n\nOPTIONAL, and its absence is meaningful rather than incidental: a deployment whose registry has no transparency log behind it has no proof to offer, and saying nothing is the honest answer. A verifier that treats a missing definitionProof as equivalent to a present one has skipped the only check that distinguishes them.\n\nIt deliberately carries no node URL. Where the log lives is the deployment's published self-description (#70), which can be corrected; an address baked into a signed credential is a redirect nobody can ever withdraw.",
              "properties": {
                "namespace": {
                  "type": "string",
                  "minLength": 1
                },
                "registry": {
                  "type": "string",
                  "minLength": 1
                },
                "record": {
                  "type": "string",
                  "minLength": 1
                },
                "version": {
                  "type": "string",
                  "minLength": 1,
                  "description": "The registry version to pin the lookup to. A pin that is not checked against what came back is not a pin — the node ignores an unrecognised parameter and answers with the latest version (DeDi-node#65)."
                },
                "digest": {
                  "type": "string",
                  "pattern": "^[0-9a-f]{64}$",
                  "description": "The record's content digest at issuance. A verifier who resolves the record later and finds a different digest has learned something important."
                }
              },
              "required": [
                "namespace",
                "registry",
                "record",
                "version",
                "digest"
              ],
              "additionalProperties": false
            },
            "skillCode": {
              "type": "string",
              "pattern": "^CREST-SKILL:[a-z0-9]+(\\.[a-z0-9-]+)*\\.v[0-9]+$",
              "description": "The skill this work evidences, copied from the definition at issuance (urn:crest:schema:reference:skill:1).\n\nCopied rather than referenced through the definition, because it is the one field whose entire purpose is to be read by somebody who is not this deployment — and a verifier who has to resolve the definition first to learn what skill this was is a verifier who cannot answer the question offline.\n\nOPTIONAL: a definition that names no skill produces a credential that names none. The work is still evidenced; it is simply not yet portable in the way §3 intends."
            },
            "activity": {
              "type": "string",
              "minLength": 1
            },
            "outcome": {
              "$ref": "urn:crest:schema:primitives:common:1#/$defs/outcome"
            },
            "period": {
              "$ref": "urn:crest:schema:primitives:common:1#/$defs/period"
            },
            "geography": {
              "type": "string"
            },
            "evidenceFields": {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1
              },
              "description": "The names of the fields the source record carried — names only, never values. A definition's tier map can require a field, and a verifier who cannot tell whether the record had it resolves a lower tier than the issuer did. Offline is the case that matters (W6), so the credential has to carry enough to answer the question, and a list of field names discloses nothing about the household they describe."
            }
          },
          "required": [
            "claimId",
            "eventId",
            "definition",
            "activity",
            "outcome",
            "period"
          ],
          "additionalProperties": false
        },
        "provenance": {
          "$ref": "urn:crest:schema:primitives:common:1#/$defs/provenance",
          "description": "The facts the strength function reads. A credential that stated its own tier would freeze a judgement verifiers must be free to make differently."
        },
        "confirmation": {
          "type": "object",
          "properties": {
            "route": {
              "type": "string",
              "enum": [
                "self",
                "auto",
                "assisted"
              ]
            },
            "at": {
              "$ref": "urn:crest:schema:primitives:common:1#/$defs/timestamp"
            }
          },
          "required": [
            "route",
            "at"
          ],
          "additionalProperties": false,
          "description": "Which route accepted the claim. A verifier can see that a credential was auto-confirmed rather than actively confirmed, and weigh it."
        },
        "issuerAuthority": {
          "type": "object",
          "description": "Who stands behind this credential, and where a verifier can check it (§3, §8).\n\n§8's sketch named `qualificationRef` and `grantRef` as two separate things. §2 had already collapsed them: Qualification and ProjectGrant became ONE Authorization at two scopes, instance-wide and context-bound, where a context grant is always a strict subset of an instance one. So these are two references to the same primitive at its two scopes, not to two primitives.\n\nEvery reference here is to an ORGANISATION's authorization. A person's is never published (#68) — it would be a permanent public record of who works where — so a chain that ended at a supervisor would end at something a verifier cannot resolve.",
          "properties": {
            "orgId": {
              "$ref": "urn:crest:schema:primitives:common:1#/$defs/did",
              "description": "The deployment operator, as published in its own instance record (#70)."
            },
            "qualificationRef": {
              "$ref": "urn:crest:schema:primitives:common:1#/$defs/id",
              "description": "The organisation's INSTANCE-scoped Authorization — the old Qualification. 'This organisation may attest work at all.'"
            },
            "grantRef": {
              "$ref": "urn:crest:schema:primitives:common:1#/$defs/id",
              "description": "The organisation's CONTEXT-scoped Authorization for this unit's project — the old ProjectGrant. Always a strict subset of the instance one."
            }
          },
          "required": [
            "orgId"
          ],
          "additionalProperties": false
        }
      },
      "required": [
        "id",
        "workEvent",
        "provenance",
        "confirmation"
      ],
      "additionalProperties": false
    },
    "credentialStatus": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "type": {
          "const": "BitstringStatusListEntry"
        },
        "statusPurpose": {
          "const": "revocation"
        },
        "statusListIndex": {
          "type": "string"
        },
        "statusListCredential": {
          "type": "string",
          "minLength": 1
        }
      },
      "required": [
        "id",
        "type",
        "statusPurpose",
        "statusListIndex",
        "statusListCredential"
      ],
      "additionalProperties": false
    },
    "proof": {
      "type": "object",
      "properties": {
        "type": {
          "const": "DataIntegrityProof"
        },
        "cryptosuite": {
          "const": "eddsa-jcs-2022",
          "description": "Ed25519 over JCS-canonicalised JSON. Chosen over the JSON-LD suite because verifying that one requires resolving a context document, and a verifier that must fetch a URL is not offline (W6)."
        },
        "created": {
          "$ref": "urn:crest:schema:primitives:common:1#/$defs/timestamp"
        },
        "verificationMethod": {
          "type": "string",
          "minLength": 1
        },
        "proofPurpose": {
          "const": "assertionMethod"
        },
        "proofValue": {
          "type": "string",
          "minLength": 1
        }
      },
      "required": [
        "type",
        "cryptosuite",
        "created",
        "verificationMethod",
        "proofPurpose",
        "proofValue"
      ],
      "additionalProperties": false
    }
  },
  "required": [
    "@context",
    "id",
    "type",
    "issuer",
    "validFrom",
    "credentialSubject",
    "credentialStatus"
  ],
  "additionalProperties": false
}
