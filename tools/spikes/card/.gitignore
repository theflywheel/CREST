# Card artefacts are regenerated by `make printed-card` from a live issuance.
# The credential itself is checked in under pkg/credential/testdata instead,
# where it is a fixture with a test attached rather than a leftover output.
*
!.gitignore
