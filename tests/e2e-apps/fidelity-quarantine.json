{
 "_note": [
  "Screens the ledger calls implemented that the gate cannot judge today,",
  "each with an issue. A quarantined screen's failures are printed and",
  "recorded, never blocking — and one that starts passing every facet FAILS",
  "the run, so a stale quarantine cannot survive the thing it was hiding",
  "being fixed.",
  "",
  "Empty today: the G-2 wave's ten entries shared one root — the onboarding",
  "screens read the in-session application (sessionStorage, written by the",
  "g2_1 register form) and the gate could only cold-load a route — and #179",
  "closed it by giving the gate scripted-flow arrivals (the flow: modes in",
  "fidelity-map.json), so every one of those screens is asserted now."
 ],
 "screens": {}
}
