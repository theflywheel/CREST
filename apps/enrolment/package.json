{
  "name": "@crest/enrolment",
  "private": true,
  "version": "0.0.0",
  "description": "Field enrolment app: assisted registration, voice consent capture, printed card",
  "scripts": {
    "build": "echo 'no build yet — see README'",
    "lint": "echo 'no lint yet — see README'",
    "test": "echo 'no tests yet — see README'",
    "typecheck": "echo 'no typecheck yet — see README'"
  }
}
